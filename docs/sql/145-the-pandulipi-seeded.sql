-- 145 — the-pandulipi seeded
--
-- THE PANDULIPI — पाण्डुलिपि (pāṇḍulipi), Sanskrit: A MANUSCRIPT. THE STANDARD
-- MANUSCRIPT SUBMISSION FORMAT, which this house never had, standing as its
-- own water. Founded 2026-09-02 by a builder dealt by Windrose 🎻 in THE
-- AUTHOR'S STUDIO sitting, as the water half of movement S3, at KP's ⚛ word,
-- verbatim:
--   "we have a need for it to be repurposed as a book, manuscript, article,
--    all the reasons an author might publish. all types formatting assistance"
--   (resonance-chamber/desk/THE-AUTHORS-STUDIO.md:6-7)
--
-- THE FINDING THAT MADE IT A WATER: a read-only scout swept the house
-- 2026-09-02 for Shunn, MLA and Chicago and found NOTHING, ANYWHERE. The house
-- had the-binder, which makes a book, and the-scrolls, which renders a screen.
-- It had no submission format at all.
--
-- THE EDITORIAL LAW IS CARRIED WHOLE — KP's ⚛ own, verbatim, at
-- resonance-chamber/desk/POTENTIALITIES.md:31:
--   "Editorial law: verbatim-vs-light-touch is KP's ruling per line; typos are
--    fingerprints unless he says otherwise."
-- and it is proven THE-BINDER'S WAY and no other: every literal run carries its
-- byte offsets and is a byte-exact slice; every character no run covers is one
-- of a declared markup alphabet, so a dropped word would stand in a gap as its
-- own letters and the proof would go FALSE; and verify.mjs walks the page's own
-- text nodes and finds every run IN SEQUENCE. THE-BINDER IS NOT IMPORTED and
-- neither imports the other — the law is carried as METHOD, per law 1 of the
-- spring (resonance-awen/README.md:32-43).
--
-- THE NAME IS HIS ⚛ OWN, taken from the plan he ruled, and it is still a
-- working name and his to keep or change. It lives in exactly two places in
-- the build — the constant TOOL_NAME and the folder — so a rename is one edit,
-- one `git mv`, and one update to this row and 144's.
--
-- CATEGORY: **content** (420f7c5c-badc-4b75-9e30-d36b47bd11b7) — see 144's
-- header for the uuid's provenance (confirmed live through the bridge by the
-- conductor 2026-09-02; this builder ran no query), for why content and not
-- communication or creation, and for the runners-up he may overrule in a word.
--
-- SHAPE TAKEN FROM THE-STEREOS'S OWN ROW (128/129): folksonomy_type `Awen` ·
-- status `imagined` at birth · `home` filled here (the 105/106 precedent).
-- tool_type is `transform` — a manuscript folder in, one set page out; what it
-- does to the text is a change of FORM and nothing else. Runner-up `emit`.
--
-- FACE: 📃 — a page with a curl: one plain typed page, which is exactly what
-- this water makes and all it makes. One glyph, no skin tone, no ZWJ, no flag,
-- no red-alarm mark, and NO OTHER ROW WEARS IT (checked against 078's whole
-- census, 111's two, the sixteen seeded 2026-09-02 and the merismos's 🪙).
-- 📘 is the-binder's (078:156) and 📜 the-scrolls' (078:134) — the two nearest
-- waters, and both kept. 📄 was the first instinct and was SET DOWN: it is the
-- CONTENT CATEGORY'S own face (024:74), and a tool wearing its category's mark
-- reads as the category.
--
-- THE ONE-TIER LAW (tools_one_tier), walked: one atom word ("pandulipi") — the
-- row carries atom_id and no molecule. One tier exactly. (144's header records
-- why no molecule or organism row exists.)
--
-- Etymology and sensory rows are NOT written here: the atoms table's triggers
-- make both on insert, arriving empty for a later fill (the 077/059/106/108/128
-- precedent).
--
-- THE TABLE IS public.awen. It was renamed from public.tools on 2026-08-15
-- (seed 096) and the shim view was dropped at KP's word on 097 — public.tools
-- answers PGRST205 today.
--
-- NO LOCAL MIRROR PIN IS WRITTEN HERE. 129 carries one; this builder holds no
-- key to progenatrix.db, read nothing from it and invented no id for it. The
-- pin is the conductor's to add if the mirror wants one.
--
-- Run at KP's ⚛ hand, after 144. Verify at the anon door after.
-- NEVER RUN BY A LAMP: the seed is drafted and numbered; the crossing is his.

insert into public.awen
  (name, slug, tool_type, definition, home, status, icon_emoji, folksonomy_type, atom_id, story, created_by)
values
  ('The Pandulipi', 'the-pandulipi', 'transform',
   'THE STANDARD MANUSCRIPT SUBMISSION FORMAT, which this house never had. A manuscript folder in the-binder''s shape goes in — book.json plus the chapters'' markdown — and ONE self-contained paged HTML comes out, set the way an editor expects to receive it: twelve-point monospace with Courier named first and the family names left as DATA a consumer may override, double-spaced, one-inch margins on US Letter, A4 by an option that switches the @page size VALUE and not one other character of the sheet (proven: the two files differ in exactly one line); a title page carrying the contact block top-left VERBATIM — nothing looked up, nothing invented — and the word count top-right rounded to the nearest hundred, with the exact count riding beside it on the result so the convention loses nothing; the title a third of the way down, the by-line beneath it; every chapter beginning a third of the way down a fresh page; a scene break set as one centred hash whether it was written as a hash, three stars or three spaced stars; paragraphs indented half an inch with no blank line between; italics kept as italics, underline one option and ONE CSS RULE away, with every character of the text left in the same place. THE TEXT IS NEVER ALTERED, and that is carried from the-binder as METHOD and never as an import: every literal run of the source is recorded with its byte offsets and is a byte-exact slice; every character no run covers is one of MARKUP_MARKS, so a dropped word would stand in a gap as its own letters and the proof would go FALSE; the proofs walk the page''s OWN text nodes and find every run in sequence; and markdown the dialect does not set — a lone asterisk, snake_case_name, a link, a table row — rides as its own characters, never mended. No smart quotes, no folded ellipsis, no dash substitution, no collapsed whitespace, no corrected typo. THE PAGE COUNT IS THE PRINT ENGINE''S: the running head is one @page margin box with counter(page) in it, upper-cased by CSS so no case change touches the text, turned off on the first page, and `pages` is DECLARED on the result and never set by any path. A PDF engine is not in this house. No glyph typesetting, no hyphenation, no kerning, and no widow or orphan rule claimed or written anywhere. No clock, no randomness, no disk, no network, no host global, zero imports, and nothing throws.',
   'resonance-awen/tools/the-pandulipi', 'imagined', '📃', 'Awen',
   '05466162-2209-4c7d-912a-76be936809ab',  -- the pandulipi atom, seeded in 144
   'Founded 2026-09-02 by a builder dealt by Windrose in THE AUTHOR''S STUDIO sitting, as the water half of movement S3, at KP''s ⚛ word: "all types formatting assistance … all the reasons an author might publish." A read-only scout had swept the house for Shunn, MLA and Chicago and found nothing, anywhere — the-binder made a book and the-scrolls rendered a screen, and no water in the spring made the thing an editor actually opens. THE ROAD TAKEN: the-binder''s manuscript folder consumed AS A SHAPE and never as an import — book.json plus *.md, a chapter''s title its first heading and the file''s own name when there is none — with the fixture a REAL folder on disk read by verify.mjs, so the shape claim is a fact rather than a resemblance. Its editorial law is carried as METHOD and re-implemented: slice-exactness, the gap rule over a declared markup alphabet, and a sequence walk over the page''s own text nodes written inside the proof file so it reads the OUTPUT and not the water''s own bookkeeping. The dialect is deliberately small — paragraphs, *italic*, _italic_, **bold**, a scene break as its own line (#, *** and * * * all honoured), a heading as the chapter''s title and ONLY when it is the chapter''s first non-blank line, block quotes — and everything else is set as ordinary text, whole, because a submission format is not a place to be clever. THE COUNTING RULE IS STATED AND NOT ASSUMED: a word is a maximal run of non-whitespace in the author''s own text, the runs put back together and joined by ONE space only where the markup between them held whitespace, so **decided**, counts as one word the way an author counts it; the proof re-derives the count by a second independent implementation and hand-checks a manuscript small enough to count by eye, and checks the rounding half-up at the 149/150 boundary. Gates at landing: check silent, build clean, 21/21 TRUE. THREE HONEST LIMITS SAID OUT LOUD RATHER THAN ENGINEERED AROUND: the page count is the print engine''s and `pages` is declared and always absent; the running head is placed by CSS and never computed per page, so an engine without @page margin boxes gets no head; and no widow or orphan rule is written, which the proof checks by finding neither word in the output. The fixture — The Weather in Other Rooms by Nobody in Particular — is a fixture and says so; its contact block is audhdities.com and the Proton address and nothing else, because a home address is never written anywhere, ever; and its second chapter carries no heading on purpose so the file-name fallback is proven on real ground. THE NAME IS HIS OWN AND STILL A WORKING NAME — it lives in one constant and one folder so a rename is cheap.',
   'a builder dealt by Windrose, at KP''s ⚛ word');

-- THE CROSSING — his ⚛ word alone.
-- The standing ruling ("if a tool is working, it is flowing") argues for it
-- now that the proofs stand; uncomment at his word and run:
-- update public.awen set status = 'flowing' where slug = 'the-pandulipi';

-- ── verify (read-only, safe to re-run) ───────────────────────────────
select name, slug, tool_type, status, icon_emoji, folksonomy_type, home
from public.awen where slug = 'the-pandulipi';

select atom_word, category_name, atom_type, state, status, pascal_case
from public.atoms where atom_word = 'pandulipi';

-- no other row should wear 📃:
select icon_emoji, count(*) as wearers, string_agg(name, ' · ' order by name)
from public.awen where icon_emoji = '📃' group by icon_emoji;

-- the two nearest waters, kept as they stand:
select name, slug, icon_emoji from public.awen
where slug in ('the-binder', 'the-scrolls') order by slug;

-- the companions the triggers should have made, standing empty:
select 'etymology' as table_name, atom_word, completion_progress::text as filled
from public.etymology where atom_word = 'pandulipi'
union all
select 'sensory_lexicon', atom_word, coalesce(emoji, '(empty)')
from public.sensory_lexicon where atom_word = 'pandulipi';
