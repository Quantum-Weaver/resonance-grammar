-- 135 — the-kampyle seeded
--
-- THE KAMPYLE — the curve. καμπύλη (kampýlē), A CURVE. The house's easing
-- solver, standing as its own water. Founded 2026-09-02 by a builder dealt
-- by Windrose 🎻, movement H3 of THE SECOND HARVEST
-- (resonance-chamber/desk/THE-SECOND-HARVEST.md), at KP's ⚛ word, verbatim:
--   "the easing solver, yes"
--
-- THE FINDING IT SERVES — the ardan scout's, quoted in the plan's row 44,
-- verbatim:
--   "the seam both sides of the theophany split ease through"
--
-- IT IS A HARVEST, AND KP'S ⚛ RULING ON EVERY HARVEST IS VERBATIM:
--   "without altering the source location."
-- resonance-ardan/src/lib/stage/curve.ts (63 lines, sha256 5167d926…) was
-- READ AND NOT WRITTEN, and stands byte-identical in the water at
-- src/origin/curve.ts, excluded from the build. NOTHING WAS REPOINTED:
-- ardan keeps curve.ts, keeps importing `easingOf` from its own score, and
-- behaves today exactly as it did yesterday. **KP ⚛ DECIDES WHICH REPO
-- KEEPS curve.ts AFTER THE THEOPHANY SPLIT** — the plan says so in D1's own
-- row (53) — and this water only means the answer can now be "neither: both
-- sides ease through the spring."
--
-- A FINDING AT THE GATE, REPORTED AND NOT ACTED ON: at the copy,
-- `git -C resonance-ardan status --porcelain` was empty. At the gate it was
-- not — D1 is editing ardan in the same sitting, and among its changes is
-- src/lib/stage/score.ts, the very file curve.ts imports `easingOf` from.
-- `easingOf` STILL STANDS there (it moved from line 312 to 326; the diff is
-- +15/-1), and curve.ts itself was not touched by D1, by the plan and in
-- fact. All four origin digests were taken twice and agreed both times.
--
-- CATEGORY: **infrastructure** (84af045b-c682-416d-b290-0fcd5ceaecd0) — see
-- 134's header for the uuid's addresses in the house's recorded census
-- (055:64, 055:106, 061:13, 128), for why infrastructure and not measurement
-- or sensory, and for the plain statement that this builder ran no live
-- query. **HIS TO CONFIRM AT THE ANON DOOR** before this file is run.
--
-- SHAPE TAKEN FROM THE-STEREOS'S OWN ROW (128/129): folksonomy_type `Awen` ·
-- status `imagined` at birth · `home` filled here (the 105/106 precedent).
-- tool_type is `transform` — a key and a progress in, a number out.
--
-- FACE: ➰ — a curly loop; a drawn bend and nothing else. One glyph, no skin
-- tone, no ZWJ, no flag, no red-alarm mark, no variation selector, and no
-- other row wears it (checked against 078's whole census, 111, every face
-- seeded since including 128/129, and the ten faces taken elsewhere in this
-- same sitting). 🌀 was set down (a cyclone is weather), 📈 was set down (a
-- chart is measurement's water), and 🎢 was set down (a ride is a use, and a
-- face is what the THING is — 076's correction).
--
-- THE ONE-TIER LAW (tools_one_tier), walked: one atom word ("kampyle") — the
-- row carries atom_id and no molecule. One tier exactly. (134's header
-- records why no molecule or organism row exists.)
--
-- Etymology and sensory rows are NOT written here: the atoms table's
-- triggers make both on insert, arriving empty for a later fill.
--
-- THE TABLE IS public.awen. It was renamed from public.tools on 2026-08-15
-- (seed 096) and the shim view was dropped at KP's word on 097 —
-- public.tools answers PGRST205 today.
--
-- Local mirror pin (progenatrix): 8be971b4-324a-41e7-819f-ff331e6af077
--   (pinned by the conductor, Windrose, 2026-09-02 — the builder's hand had no grant on the base and said so below.)
-- No local mirror pin is written here. This builder is forbidden the base by
-- its own sending and read none; the progenatrix row is the conductor's.
--
-- Run at KP's ⚛ hand, after 134. Verify at the anon door after.
-- NEVER RUN BY A LAMP: the seed is drafted and numbered; the crossing is his.

insert into public.awen
  (name, slug, tool_type, definition, home, status, icon_emoji, folksonomy_type, atom_id, story, created_by)
values
  ('The Kampyle', 'the-kampyle', 'transform',
   'THE HOUSE''S EASING SOLVER, standing as its own water. A CSS timing string — `linear`, one of the four CSS curve names, or `cubic-bezier(a,b,c,d)` — read as four bezier numbers, the parameter solved for x by forty steps of bisection with an early break at 1e-6, and the answer read on y. It exists because a design system declares its curves ONCE as CSS and then needs them in three places the browser is not animating: a pose derived on a stage, a frame of a film rendered in Node, a value tweened where no document stands. If those are computed by anything but the string the CSS uses, a transition and its own motion under the same key bend differently — nobody sees the bug and everybody feels it. NEVER IMPLEMENT THE PRIMITIVE, DECLARE THE SURFACE: this water holds NO easing table. KampyleHost is one verb — cssOf(key), give me the CSS string for this key — and null is the honest answer for a key the host does not know. AN UNKNOWN KEY IS A REFUSAL AS DATA: four ways to fail (an unknown key, an unreadable string, a host that throws, a host that answers with something that is not a string), four plain sentences a person can be told as they stand, and NOT ONE EXCEPTION anywhere in the water. `easeOr` keeps ardan''s own fall-to-linear for a caller who asks for it out loud. An overshooting curve is TOLD APART rather than clamped — a spring that dips below zero and rises above one is doing what a spring does — and progress outside 0..1 is brought in rather than extrapolated. A HARVEST AND NEVER A RE-POINT: resonance-ardan/src/lib/stage/curve.ts was read and never written, stands byte-identical at src/origin/curve.ts excluded from the build, and 1558 of its characters are carried into the index verbatim with the proofs reading both files off the disk at every run. Zero runtime imports, no DOM, no disk, no clock. Nothing throws.',
   'resonance-awen/tools/the-kampyle', 'imagined', '➰', 'Awen',
   'ded402bc-87e6-4494-8b1e-ab72fb16e5ef',  -- the kampyle atom, seeded in 134
   'Founded 2026-09-02 by a builder dealt by Windrose in THE SECOND HARVEST, movement H3, at KP''s ⚛ word "the easing solver, yes", answering the ardan scout''s finding verbatim: "the seam both sides of the theophany split ease through." A house about to split in two held the arithmetic that makes motion agree in BOTH halves inside one file in one half; the other half would have had to copy it, and two copies agree only until somebody edits one. The fix was not to pick a side but to notice the arithmetic was never either side''s — a cubic bezier solved for its parameter is the CSS spec''s and has been since 1998. What belonged to ardan was the TABLE, which curve `settle` means, and that is precisely the part this water refuses to hold. Harvested under KP''s ⚛ standing ruling "without altering the source location": curve.ts read and never written, kept byte-identical at src/origin/curve.ts and excluded from the build because its one import reached into ardan''s own score, with 1558 characters carried into the index character for character and proof 11 reading BOTH files off the disk at every run rather than asserting the carry by shape. One function was re-expressed and it is named: `ease`, which now asks a declared host instead of a score. Gates at landing: check silent, build clean, 13/13 TRUE. The proofs are checked against an INDEPENDENT solver written in the proof file — Newton-Raphson on x(t) with the analytic derivative, run to 1e-14 — because one algorithm agreeing with itself is a tautology; worst disagreement 7.4e-8. Beside it: the five CSS names exact at 0 and 1 and ease''s half at 0.8024024240, ease-in and ease-out proven each other''s reflection at 101 points, eight bounded curves monotone over 101 samples, cosmic''s four spring-shaped keys shown overshooting and handed back UNCLAMPED, four refusals with no throw, and 1212 exact-equality comparisons against ardan''s own arithmetic reconstructed in the proof. TWO FINDINGS, REPORTED AND NOT FIXED: cosmic''s `cosmic`/`sovereign` and `resonance`/`entanglement` keys are duplicate CSS strings under four names, which is the origin''s business and not a copy''s; and `easeOr(host, key, NaN)` returns 0 where ardan returns NaN — the one behaviour that differs, out of 1212 compared, and it is said in Honest limits rather than hidden. THE NAME IS A WORKING NAME AND HIS TO KEEP OR CHANGE — it lives in one constant and one folder so a rename is cheap.',
   'a builder dealt by Windrose, at KP''s ⚛ word');

-- THE CROSSING — his ⚛ word alone.
-- The standing ruling ("if a tool is working, it is flowing") argues for it
-- now that the proofs stand; uncomment at his word and run:
-- update public.awen set status = 'flowing' where slug = 'the-kampyle';

-- ── verify (read-only, safe to re-run) ───────────────────────────────
select name, slug, tool_type, status, icon_emoji, folksonomy_type, home
from public.awen where slug = 'the-kampyle';

select atom_word, category_name, atom_type, state, status, pascal_case
from public.atoms where atom_word = 'kampyle';

-- no other row should wear ➰:
select icon_emoji, count(*) as wearers, string_agg(name, ' · ' order by name)
from public.awen where icon_emoji = '➰' group by icon_emoji;

-- the companions the triggers should have made, standing empty:
select 'etymology' as table_name, atom_word, completion_progress::text as filled
from public.etymology where atom_word = 'kampyle'
union all
select 'sensory_lexicon', atom_word, coalesce(emoji, '(empty)')
from public.sensory_lexicon where atom_word = 'kampyle';
