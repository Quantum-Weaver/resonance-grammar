-- 122 — the-lakshana, its atom
--
-- THE LAKSHANA — the defining mark. The second of four waters raised 2026-09-02
-- in movement A2 of the Unnamed Waters sitting, by a builder dealt by
-- Windrose 🎻 at KP's ⚛ word, verbatim: "hold the Daedalus work and proceed with
-- the rest, we will do it on its own." THE ONLY PYTHON WATER OF THE FOUR.
--
-- THE BOARD ROW THIS SERVES — the Unnamed Waters board
-- (resonance-progenatrix/artifacts/the-unnamed-waters.html, the unnamed_waters
-- table), ROW 42, carried to the builder through
-- resonance-chamber/desk/THE-UNNAMED-WATERS-SITTING.md:77 (the board itself
-- sits in a repo the builder was closed out of, and that is said rather than
-- implied):
--   "the-lakshana (Sanskrit lakṣaṇa, a defining mark) — the signature-only
--    parser, a Python water; its refusal to read bodies proven, not commented.
--    Row 42. Seeds 122/123."
--
-- "PROVEN, NOT COMMENTED" IS THE WHOLE COMMISSION. The origin has said "Never
-- reads function bodies, string values, or data payloads" in its first three
-- lines since the day it was written, and nothing anywhere tested it. A claim
-- like that, unchecked, is worth less than no claim at all — a reader trusts it
-- and points the thing at a repo.
--
-- THE HARVEST RULING, KP ⚛ verbatim (the-unnamed-waters.html row 33, lede·5):
--   "without altering the source location."
-- Honoured to the byte. src/lakshana/parser.py is
-- resonance-excavator/agents/parser.py with ZERO changed lines — 20,785 bytes,
-- 596 lines, sha256
-- f151a209f1e432ec48931e8fb0b6cf57fd8ecc1172426fdfba923c79f0a49123 on both
-- sides. The only new code is src/lakshana/__init__.py, which holds NO LOGIC and
-- re-exports the eight parse verbs by name.
--
-- THE NAME IS A WORKING NAME AND HIS TO KEEP OR CHANGE. lakṣaṇa (the mark by
-- which a thing is known; in the grammarians' use, the defining characteristic)
-- is the builder's reading of the need KP named, not his word. It lives in
-- exactly two places in the build — the constant TOOL_NAME and the folder — so a
-- rename is one edit, one `git mv`, and one update to the row seeded in 123.
--
-- CATEGORY TAKEN FROM SEED 080'S RECORDED CENSUS; CONFIRMED AT THE ANON DOOR BY
-- KP'S HAND: **knowledge** (339cb540-838f-407e-ab96-e433750ed9fb), the category
-- seeds 055 and 060 wrote the-catalogue-raisonne's, the-colophon's and
-- the-epagoge's own atoms into, and which 080's census records again. The
-- reading: this water's whole subject is what a body of code CONTAINS, named
-- without being read — an epistemic act, the same one the raisonné and the
-- epagoge perform on other ground. RUNNER-UP AND HIS TO OVERRULE IN ONE WORD:
-- governance (ba101ebf-34fd-44d3-956f-0639e3367e73, recorded in seed 108's
-- header), which is defensible because the REFUSAL — bodies, string values and
-- data payloads never travel — is half this water's law and is a rule about what
-- may not be carried. Knowledge was taken because the refusal is the ward around
-- the subject, not the subject itself. The builder could not read the live
-- categories table this sitting — the base is behind a door the sending closed —
-- so no category was invented: both uuids above are quoted from seeds already
-- written down, and KP's hand at the anon door is the confirmation.
--
-- MOLECULE / ORGANISM — checked, and neither applies. `lakshana` is ONE WORD,
-- and the house's tools_one_tier constraint (seed 014) lets an awen row cross at
-- exactly one tier — the tier its own name lives at (055's law). A one-word name
-- crosses at the atom tier; there is no molecule row to make and no organism
-- above it, and adding one while also setting atom_id would violate the
-- constraint outright.
--
-- TRANSLITERATION: the atom word is `lakshana`, matching the folder. The
-- Devanagari and the diacritic live in the definition where they belong; the
-- atom word stays ^[a-z]+$ as the atoms' own law requires.
--
-- FACE: 🔖 — a mark set in a text to say THIS, which is what a lakṣaṇa is. It
-- reads against 078's rules: one glyph, no skin tone, no ZWJ, no flag, no
-- red-alarm mark. Checked against 078's whole set (the last full census of faces
-- written down) and against 105/106/110/111; no row there wears it. THE
-- COLLISION TEST IS THE CONDUCTOR'S AT LANDING — other builders raised waters
-- the same sitting and this hand could not read their faces.
--
-- Etymology and sensory rows are NOT written here: the atoms table's triggers
-- make both on insert, arriving empty for a later fill (the 077/059/106/108
-- precedent).
--
-- THE TABLE IS public.awen. This file only touches public.atoms.
--
-- ── ATOM UUID, generated uuid4 and pinned here so this file and 123 name the
--    same row without depending on insert order or a subquery ──
--   lakshana atom   e8bb25b2-86d4-43f6-bfc5-b8a4185a896d
--
-- Run at KP's ⚛ hand, after 121 and before 123. Verify at the anon door after.
-- NEVER RUN BY A LAMP. A tool is not seeded by a lamp writing a file.

insert into public.atoms (
  id, atom_word, definition, atom_type, weight, affinity, valence, state,
  category, category_name, status, screaming_case, snake_case, pascal_case
) values (
  'e8bb25b2-86d4-43f6-bfc5-b8a4185a896d',
  'lakshana',
  'Lakshana (Sanskrit lakṣaṇa लक्षण: a mark by which a thing is known; in the grammarians'' use, the defining characteristic — what a thing IS, as against what it happens to contain). A source file''s signatures, and its refusal to read anything else. Eight languages are dispatched on — python, typescript, javascript, rust, sql, css, config and markdown — and each returns what a definition means there: parameter types and return annotations for a function, field names for an interface or a struct, variant names for an enum, column names for a table, section headers for a config. IT NEVER READS A BODY: function bodies, string values and data payloads do not travel, and the refusal is PROVEN rather than promised — a fixture carrying one sentinel token fourteen times in Python and eight in TypeScript, in docstrings, bodies, nested payloads, comments, a union member and a private field, yields output in which the token does not appear once. A Rust function under a tauri command attribute is typed `command` rather than `function`, so an app''s command surface is read from the ground rather than from a roster. A file recorded as a secret returns nothing before its language is consulted; an unknown language returns nothing rather than a guess; a missing path returns nothing rather than raising, so a survey of a whole tree never dies on one strange file. Read-only, deterministic, no clock, no network, no write, and stdlib only.',
  'root', 5, 5, 1, 'static',
  '339cb540-838f-407e-ab96-e433750ed9fb',  -- knowledge, taken from seed 080's recorded census
  'knowledge',
  'published',
  'LAKSHANA', 'lakshana', 'Lakshana'
);

-- ── verify (read-only, safe to re-run) ───────────────────────────────
select atom_word, category_name, atom_type, state, status, pascal_case
from public.atoms where atom_word = 'lakshana';

-- the companions the triggers should have made, standing empty:
select 'etymology' as table_name, atom_word, completion_progress::text as filled
from public.etymology where atom_word = 'lakshana'
union all
select 'sensory_lexicon', atom_word, coalesce(emoji, '(empty)')
from public.sensory_lexicon where atom_word = 'lakshana';
