-- 118 — the-raksha, its atom
--
-- THE RAKSHA — protection. The pwned check, built 2026-09-02 by a builder
-- dealt by Windrose 🎻 at KP's ⚛ word, verbatim: "hold the Daedalus work and
-- proceed with the rest, we will do it on its own." Movement A1 of the Unnamed
-- Waters sitting, board row 38
-- (resonance-chamber/desk/THE-UNNAMED-WATERS-SITTING.md:73), whose own words
-- are the specification: "the pwned check; digest and fetch as a declared host
-- surface; fail-open proven."
--
-- THE HARVEST RULING THIS ROW RESTS ON — KP ⚛, verbatim, board row 33:
--   "without altering the source location"
-- AudHDities/src/lib/auth/pwned.ts (39 lines) was COPIED byte-verbatim;
-- AudHDities is untouched and still runs it. The SHA256 of the copy is pinned
-- in the water's verify.mjs and re-checked every run.
--
-- THE HARVEST IS CARRIED WHOLE AND LEFT OFF THE DOOR, and that is the design
-- decision this row records. The origin wires both primitives straight into
-- one function — crypto.subtle.digest and fetch, inside the thing that answers
-- the question. Right for an app, wrong for a standalone water, because the
-- spring's law is the-clavis's: NEVER IMPLEMENT THE PRIMITIVE, DECLARE A HOST
-- SURFACE. So the file stands in src/pwned.origin.ts unedited as the
-- provenance of everything above it, and the water's own pwnedCount takes TWO
-- arguments where the origin's takes one — proof 9 checks the arity, so the
-- wired function is PROVABLY unreachable through the door. The one piece that
-- travels is PWNED_MESSAGE, re-exported rather than retyped so the sentence a
-- person reads cannot drift from what the app shows today.
--
-- CATEGORY: **identity** (47cb9e9a-65da-48b7-aef1-88fced80ca85) — category
-- taken from seed 107's recorded census; confirmed at the anon door by KP's
-- hand. Its live definition, quoted in 107's own header (categories table,
-- sort_order 21): "The marks of being someone — names, handles, signatures,
-- traits. How a self shows up." A password is the mark a self shows up WITH,
-- and this water asks whether that mark is already public. It stands where
-- the-signet, the-sphragis, the-clavis and the-lok stand, which is the honest
-- neighbourhood.
--   RUNNER-UP, AND THIS ONE IS WORTH HIS EYE: **moderation** ("The boundary
--   words — filters, exclusions, protections. Boundaries as safety, the
--   smallest vocabulary in the Grammar", seed 024) reads closer to the WORD
--   raksha than identity does, and **grants** ("The keys and permissions —
--   access, tokens, authentication") reads closer to the JOB. Neither was
--   taken, and the reason is not about meaning: NEITHER UUID IS RECORDED in
--   seeds 077–080 or 105–110, this hand could not reach the live categories
--   table, and writing an unverified uuid into a seed is a guess this house
--   refuses. If KP prefers either, the change is one uuid and one word, here
--   and in 119.
--
-- MOLECULE / ORGANISM — checked, and neither applies. `raksha` is ONE WORD,
-- and tools_one_tier (seed 014) lets an awen row cross at exactly one tier —
-- the tier its own name lives at (055's law). A one-word name crosses at the
-- atom tier; there is no molecule to make and no organism above it.
--
-- FACE: 🛡️ — the shield, which is what the word means. One glyph, no skin
-- tone, no ZWJ, no flag, no red-alarm mark (a shield is a position, not a
-- verdict), and NO AWEN ROW WEARS IT (checked against 078's whole census and
-- the faces set since: 🗝️ 🔒 🪙). It IS worn elsewhere in the base — by the
-- `moderation` CATEGORY (024), by the atom `ward` (004) and by Themis among
-- the deity senses (073) — all in other tables, and all meaning exactly what
-- it means here.
--
-- THE NAME IS A WORKING NAME AND HIS TO KEEP OR CHANGE. Sanskrit रक्षा
-- (rakṣā) — protection, guarding, the keeping-safe of something entrusted — is
-- this hand's reading of the need the board named. It lives in exactly two
-- places in the build (the constant TOOL_NAME and the folder).
--
-- THE NAME SWEEP, and it is the cleanest of the four: `raksha` is a TRUE ZERO
-- among the 69 folders under resonance-awen/tools/, among the atom words in
-- this seed corpus, and across every workspace *.md excluding
-- node_modules/.next/.git — ONE FILE hits, and it is the sitting's own
-- commission line. Spoken here first.
--
-- Etymology and sensory rows are NOT written here: the atoms table's triggers
-- make both on insert, arriving empty for a later fill (077/059/106).
--
-- THE TABLE IS public.awen (096; shim dropped 097). This file only touches
-- public.atoms.
--
-- ── ATOM UUID, generated uuid4 and pinned here so this file and 119 name the
--    same row without depending on insert order or a subquery ──
--   raksha atom   829ab7d6-b6b9-43f8-9118-fc488f72dc5f
--
-- Run at KP's ⚛ hand, after 117 and before 119. Verify at the anon door after.

insert into public.atoms (
  id, atom_word, definition, atom_type, weight, affinity, valence, state,
  category, category_name, status, screaming_case, snake_case, pascal_case
) values (
  '829ab7d6-b6b9-43f8-9118-fc488f72dc5f',
  'raksha',
  'Raksha (Sanskrit रक्षा: protection, guarding, the keeping-safe of something entrusted). One question — how many known breaches does this password appear in — and three answers: a number, 0 for clean, and NULL for "could not check", which is never "breached". NEVER IMPLEMENT THE PRIMITIVE, DECLARE A HOST SURFACE: it computes no hash and opens no socket. Two verbs name the machine that does, a SHA-1 over bytes and a fetch of one five-character range, and the tool supplies neither; reference hosts on two different mechanisms ship as separate files so the same answer can be shown coming off two machines. FAIL-OPEN ALWAYS: a host that returns null, throws, times out, answers a short digest or answers rubbish yields null, because a checker that locked someone out of their own vessel while a third party was down would be doing the opposite of protection — and there is no parameter anywhere that turns that off. K-ANONYMITY: exactly five uppercase hex characters of the digest leave the machine, twenty bits of one hundred and sixty, one bucket in 1,048,576, and the remaining thirty-five are compared LOCALLY against the list the host brings back; the full hash and the plaintext reach the host never, and the proofs record every string the host was handed to check it rather than describe it. IT COMPARES AND IT DOES NOT ADVISE: no verb blocks a sign-up, scores a password or measures strength. A lookup is not a judgement — a password absent from every breach list can still be terrible. Nothing is stored, remembered or logged, and a log line about a password check is a log line about a password. No clock, no disk, zero imports.',
  'root', 5, 5, 1, 'static',
  '47cb9e9a-65da-48b7-aef1-88fced80ca85',  -- identity: "The marks of being someone — names, handles, signatures, traits. How a self shows up."
  'identity',
  'published',
  'RAKSHA', 'raksha', 'Raksha'
);

-- ── verify (read-only, safe to re-run) ───────────────────────────────
select atom_word, category_name, atom_type, state, status, pascal_case
from public.atoms where atom_word = 'raksha';

-- the companions the triggers should have made, standing empty:
select 'etymology' as table_name, atom_word, completion_progress::text as filled
from public.etymology where atom_word = 'raksha'
union all
select 'sensory_lexicon', atom_word, coalesce(emoji, '(empty)')
from public.sensory_lexicon where atom_word = 'raksha';
