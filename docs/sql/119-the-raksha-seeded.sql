-- 119 — the-raksha seeded
--
-- THE RAKSHA — protection. Built 2026-09-02 by a builder dealt by Windrose 🎻
-- at KP's ⚛ word, verbatim: "hold the Daedalus work and proceed with the rest,
-- we will do it on its own." Movement A1 of the Unnamed Waters sitting, board
-- row 38, whose own words are the specification: "the pwned check; digest and
-- fetch as a declared host surface; fail-open proven."
--
-- THE HARVEST RULING — KP ⚛, verbatim, board row 33: "without altering the
-- source location." AudHDities/src/lib/auth/pwned.ts (39 lines) was copied
-- byte-verbatim; AudHDities is untouched and still runs it. HARVEST.md in the
-- folder records the byte count and the SHA256, and verify.mjs proof 10
-- re-checks the digest on every run.
--
-- THE HARVEST IS CARRIED WHOLE AND LEFT OFF THE DOOR — 118's header carries
-- the reasoning. The water's own pwnedCount takes TWO arguments where the
-- origin's takes one, and proof 9 checks the arity, so the origin's wired
-- function is PROVABLY unreachable through this water's surface.
--
-- THE PROOFS TOUCH NO WIRE. Every fetchRange in the proof file is a stub
-- answering out of a table, and the stub RECORDS EVERY STRING IT WAS HANDED,
-- so the k-anonymity contract is checked rather than described. The reference
-- hosts are exercised for their DIGEST only, which is local. The SHA-1 values,
-- the prefixes, the suffixes and the body format are real; THE BREACH COUNTS
-- ARE SYNTHETIC AND THE PROOF FILE SAYS SO IN ITS OWN WORDS, because this
-- house does not hold a copy of that list and inventing one and calling it
-- ground would be the exact dishonesty a protection tool must not model.
--
-- CATEGORY: **identity** (47cb9e9a-65da-48b7-aef1-88fced80ca85) — category
-- taken from seed 107's recorded census; confirmed at the anon door by KP's
-- hand. See 118's header for the definition matched and for the two runner-ups
-- (moderation, grants) left there for him to overrule in one word, with the
-- honest reason neither was taken: neither uuid is recorded in seeds 077–080
-- or 105–110 and this hand could not reach the live categories table.
--
-- TOOL_TYPE: **witness** — it looks at something, reports what it sees, and
-- changes nothing; it explicitly does not advise, block or score. RUNNER-UP
-- AND HIS TO OVERRULE IN ONE WORD: `capture` was considered and set aside —
-- nothing here captures anything, and nothing is stored.
--
-- FACE: 🛡️ — the shield, which is what the word means, and a position rather
-- than a verdict. One glyph, no skin tone, no ZWJ, no flag, no red-alarm mark,
-- and no awen row wears it. It IS worn by the `moderation` CATEGORY (024), the
-- atom `ward` (004) and Themis in the deity senses (073) — other tables, same
-- meaning. 078's shared-face test does not arise: no awen row shares it.
--
-- THE ONE-TIER LAW (tools_one_tier), walked: one atom word ("raksha") — the
-- row carries atom_id and no molecule. One tier exactly.
--
-- Etymology and sensory rows are NOT written here (triggers make them, 118).
--
-- THE TABLE IS public.awen (096; shim dropped 097).
--
-- Local mirror pin (progenatrix): e3bbd3c7-e344-4c29-95dd-1ce2b7002aeb
--
-- GATE AT LANDING, run from a clean shell before this file was written:
--   npm install && npm run build && npm run check && npm run prove
--   → check silent, 10/10 TRUE, deterministic, and NO NETWORK TOUCHED
--
-- Run at KP's ⚛ hand, after 118. Verify at the anon door after.

insert into public.awen
  (name, slug, tool_type, definition, home, status, icon_emoji, folksonomy_type, atom_id, story, created_by)
values
  ('The Raksha', 'the-raksha', 'witness',
   'Protection — has this password already been spilled? One job and three answers: a count of the known breaches it appears in, 0 for clean, and null for COULD NOT CHECK, which is never "breached". NEVER IMPLEMENT THE PRIMITIVE, DECLARE A HOST SURFACE: this water computes no hash and opens no socket. RakshaHost names the machine that does — digestSha1(bytes) and fetchRange(prefix5) — and the tool supplies NEITHER; two reference hosts ship as separate files on two different mechanisms (crypto.subtle plus fetch, and node:crypto plus a fetch you hand it, which is what makes the whole water provable without a wire), because a spring water hand-rolling SHA-1 would be the most dangerous thing in this house. FAIL-OPEN ALWAYS, and it is the origin''s own law carried whole: a null fetch, a DNS error, a digest that throws, a digest of the wrong length, a host missing a verb, a null host and an empty object ALL return null, nothing throws, and there is no options object anywhere that turns it off — a checker that locked someone out of their own vessel because a third party was down would be doing the opposite of protection. K-ANONYMITY, CHECKED RATHER THAN DESCRIBED: exactly five uppercase hex characters leave, twenty bits of one hundred and sixty, one bucket in 1,048,576, and the stubbed host in the proofs records EVERY string it was handed so no full digest, no suffix and no plaintext can be among them. The comparison is local, and a caller holding a cached body, a mirror or an offline list can do it with no host at all. The bytes are the password''s own and nothing normalises them — UTF-8 computed here as arithmetic and checked against known vectors, so a leading space or a capital letter hashes to something else, as it must. IT COMPARES AND IT DOES NOT ADVISE: no exported name blocks, rejects, scores, measures strength, allows or denies. A lookup is not a judgement, the host still learns a bucket and that is stated rather than waved away, and nothing is cached, retried, stored or logged. No clock, no disk, zero imports.',
   'resonance-awen/tools/the-raksha', 'imagined', '🛡️', 'Awen',
   '829ab7d6-b6b9-43f8-9118-fc488f72dc5f',  -- the raksha atom, seeded in 118
   'Built 2026-09-02 by a builder dealt by Windrose 🎻, at KP''s ⚛ word "hold the Daedalus work and proceed with the rest, we will do it on its own" — movement A1 of the Unnamed Waters sitting, four waters from the harvests, this one the fourth and the only one that grew a host surface. HARVESTED UNDER HIS OWN RULING, verbatim: "without altering the source location" — AudHDities/src/lib/auth/pwned.ts (39 lines) copied BYTE-VERBATIM, zero lines changed, AudHDities untouched and still running it. THE HARVEST IS CARRIED WHOLE AND LEFT OFF THE DOOR: the origin wires crypto.subtle and fetch straight into the function that answers the question, which is right for an app and wrong for a standalone water, so the file stands in src/pwned.origin.ts unedited as the provenance of everything above it and proof 9 checks the ARITY of this water''s own pwnedCount (two arguments, not one) so the wired function is provably unreachable through the door. The one piece of the origin that travels is PWNED_MESSAGE, RE-EXPORTED RATHER THAN RETYPED, so the sentence a person reads cannot drift from what the app shows today. The one thing added is a UTF-8 encoder, because the host surface takes bytes and something must make them — it is arithmetic and not a primitive, and every byte of it is checked against known vectors including a four-byte emoji and a lone surrogate. NO PROOF TOUCHES THE NETWORK: every fetchRange is stubbed, the stub records every string it was handed so the k-anonymity claim is checked rather than shaped, the reference hosts are exercised for their local digest only, and the remote (Have I Been Pwned''s range API) is NAMED AND NEVER DIALLED. The SHA-1s, prefixes, suffixes and body format are real and the breach counts are SYNTHETIC AND SAID TO BE. Gate at landing from a clean shell: check silent, 10/10 TRUE. THE NAME IS A WORKING NAME AND HIS TO KEEP OR CHANGE.',
   'a builder dealt by Windrose 🎻, at KP''s ⚛ word');

-- THE CROSSING — his ⚛ word alone.
-- The standing ruling ("if a tool is working, it is flowing") argues for it
-- now that the proofs stand; uncomment at his word and run:
-- update public.awen set status = 'flowing' where slug = 'the-raksha';

-- ── verify (read-only, safe to re-run) ───────────────────────────────
select name, slug, tool_type, status, icon_emoji, folksonomy_type, home
from public.awen where slug = 'the-raksha';

select atom_word, category_name, atom_type, state, status, pascal_case
from public.atoms where atom_word = 'raksha';

-- no other row should wear 🛡️:
select icon_emoji, count(*) as wearers, string_agg(name, ' · ' order by name)
from public.awen where icon_emoji = '🛡️' group by icon_emoji;

-- the four faces this sitting added, and nobody should share one:
select icon_emoji, count(*) as wearers, string_agg(name, ' · ' order by name)
from public.awen where icon_emoji in ('🎲', '🌈', '⚖️', '🛡️')
group by icon_emoji order by icon_emoji;

-- the companions the triggers should have made, standing empty:
select 'etymology' as table_name, atom_word, completion_progress::text as filled
from public.etymology where atom_word = 'raksha'
union all
select 'sensory_lexicon', atom_word, coalesce(emoji, '(empty)')
from public.sensory_lexicon where atom_word = 'raksha';
