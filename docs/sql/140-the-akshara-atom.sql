-- 140 — the-akshara, its atom
--
-- THE ORDER THIS FILE KEEPS: atom seed BEFORE awen row, and as its own
-- numbered file, in 077-the-promenade-atom.sql's exact shape and 128's
-- (the-stereos) most recent reading of it. The awen row stands as
-- 141-the-akshara-seeded.sql and runs after this one.
--
-- THE AKSHARA — the letter. akṣara (Sanskrit), A LETTER, a syllable; and read
-- as a-kṣara, THE IMPERISHABLE, that which does not wear away. Founded
-- 2026-09-02 by a builder dealt by Windrose 🎻 in the Second Harvest sitting,
-- movement H4, at KP's ⚛ word, verbatim: "Real but unasked-for — yes to both".
--
-- THE RULING IT SERVES, AND THE HONEST STATE OF THE NEED, BOTH IN HIS OWN
-- WORDS — resonance-chamber/desk/THE-SECOND-HARVEST.md, header and row 49:
--   "Real but unasked-for — yes to both"
--
-- THE SECOND HALF OF THAT RULING IS THIS ROW'S GROUND AND IS NOT SOFTENED
-- HERE. *Unasked-for* means what it says: nothing in the house is calling this
-- today. It was harvested because the code is real, proven, dependency-free
-- and load-bearing where it stands — not because a consumer asked. THE NEED IS
-- A GUESS UNTIL SOMEONE ASKS, and the nearest guess is named rather than
-- implied: resonance-sceal's title-card fonts are four HARDCODED CSS family
-- stacks at src/lib/titles.ts:62-72, resolved by fontFamily() at :83-85. That
-- is the correct design for the house's own faces. The day sceal — or ardan,
-- or the binder, or anything else — wants THE USER'S OWN font file in that
-- list, it will need exactly this, and there will be no second place to get
-- it. Until then this water is a standing answer to a question nobody has
-- asked yet, and saying otherwise would be inventing a consumer.
--
-- IT IS A HARVEST, AND KP'S ⚛ RULING ON EVERY HARVEST IS VERBATIM:
--   "without altering the source location."
-- src/sfnt.ts is a byte-identical copy of
-- resonance-cruthu/src/lib/type/sfnt.ts — the whole file, 627 lines,
-- sha256 424ddbea73df6e03d36e2046f9305e7a610a405254ca90365239312ac983a80f,
-- and the copy's digest is the same digest. The origin was READ AND NOT
-- WRITTEN (`git -C resonance-cruthu status --porcelain` empty at landing).
-- Its own RUN-LOG:14 records it proven against all 339 fonts in
-- C:/Windows/Fonts. resonance-awen/tools/the-akshara/HARVEST.md carries both
-- digests and the command that re-proves them.
--
-- NO PLATFORM SURFACE — CONFIRMED, NOT ASSUMED, AND THAT IS A FINDING. The
-- origin takes a Uint8Array and returns a plain object: no DOM, no fetch, no
-- FileReader, no FontFace, no TextDecoder, no node:*, no disk, no clock, no
-- network. It decodes UTF-16BE by hand (origin:203) and Mac OS Roman from
-- Apple's own 128-character upper half written out as a string literal
-- (origin:215). The proof is the build itself — tsconfig carries "types": []
-- and lib ES2020, and the verbatim copy compiles with NOTHING declared beside
-- it. There is no platform.d.ts in that folder, and its absence is the proof.
-- (Compare the-cofra, born the same sitting from the same repo, which needed
-- six globals hand-declared.)
--
-- NO FONT SHIPS WITH THE WATER, AND THE REASON IS A LICENCE, NOT A SHORTCUT.
-- The plan allowed transcribing two small fonts into fixtures/. None was
-- copied: the fonts on this machine are C:/Windows/Fonts and are Microsoft's,
-- whose licence covers use on the licensed machine and does NOT grant
-- redistribution — and a file in a git repository IS redistribution. A free
-- face (SIL OFL, Apache-2.0) was not fetched: the build had no network beyond
-- `npm install`, and a licence taken on trust is exactly what a
-- provenance-keeping house must not do. So the plan's stated alternative was
-- taken and carried further than it had to be: the proofs SYNTHESISE REAL
-- SFNT FILES byte by byte, with correct table checksums recomputed and
-- verified, a head carrying its true checkSumAdjustment, and a ttcf wrapping
-- two of them. One real third-party font IS read — C:/Windows/Fonts/arial.ttf,
-- BY ABSOLUTE PATH, never copied, with the proof printing a SKIP line naming
-- the gap where the path is absent.
--
-- THE NAME IS A WORKING NAME AND HIS TO KEEP OR CHANGE. akṣara is this hand's
-- reading of the need the board named, not his word. It lives in exactly two
-- places in the build — the constant TOOL_NAME and the folder — so a rename is
-- one edit, one `git mv`, and one update to this row and 141's.
--
-- ── CATEGORY ─────────────────────────────────────────────────────────
-- **sensory** (5ab76ea1-1092-4090-b0e8-e1d7dc0d5563).
--
-- THE UUID IS TAKEN FROM THE HOUSE'S OWN RECORDED CENSUS AND FROM THE
-- CONDUCTOR'S LIVE READING OF 2026-09-02, NOT FROM A QUERY THIS BUILDER RAN.
-- Its address where a hand that DID read the live table wrote it down:
-- 114-the-dath-atom.sql:94, the-dath's own row, with the category's
-- description quoted there verbatim — "The channels of perception — sight,
-- sound, touch, motion — and the visual language interfaces speak through
-- them." **CONFIRM AT THE ANON DOOR BY KP'S ⚛ HAND** before this file is run.
--
-- WHY SENSORY: the-dath is the precedent and the twin. Colour measured is
-- sensory; type identified is sensory for the same reason — typography IS the
-- visual language the description names, and this water is how a machine finds
-- out which one it is holding.
--
-- RUNNER-UP, AND HIS TO OVERRULE IN ONE WORD: **knowledge**
-- (339cb540-838f-407e-ab96-e433750ed9fb), which the-hlutr, the-lakshana and
-- the-drishti all took. It is defensible — this water produces a DESCRIPTION
-- of a file and nothing visible. It was set down because what is described is
-- a letterform, and a letterform is seen.
--
-- MOLECULE / ORGANISM — checked, and neither applies. `akshara` is ONE WORD,
-- and the house's tools_one_tier constraint (seed 014) lets an awen row cross
-- at exactly one tier — the tier its own name lives at (055's law). A one-word
-- name crosses at the ATOM tier; there is no molecule row to make and no
-- organism above it, and adding one while also setting atom_id would violate
-- the constraint outright. The-promenade, the-sphragis, the-merismos and
-- the-stereos all stand exactly this way.
--
-- TRANSLITERATION, SAID PLAINLY: the atom word is written `akshara`, the plain
-- ASCII transliteration, not `akṣara`. The house's atoms carry
-- screaming/snake/pascal cases and a slug, and a retroflex ṣ in any of them
-- would be a trap in a URL, a filename and a grep. The diacritical form is
-- kept in the definition and in the README, where it belongs. The-lakshana
-- (lakṣaṇa) and the-drishti (dṛṣṭi) were both written the same way, and this
-- follows them rather than inventing a second convention.
--
-- SHAPE TAKEN FROM THE-DATH'S OWN ROW (114/115) AND THE-STEREOS'S (128/129):
-- folksonomy_type `Awen` · status `imagined` at birth · `home` filled in 141
-- (the 105/106 precedent). tool_type is `witness`, the-dath's and
-- the-drishti's: this water observes a file and REPORTS what it found,
-- including when what it found is that the file cannot be read. The house's
-- own verb marks give witness 🧿.
--
-- FACE: 🔠 — letters themselves, which is what an akshara IS. It reads against
-- 078's rules: one glyph, no skin tone, no ZWJ sequence, no flag, no red-alarm
-- mark. NO OTHER ROW WEARS IT — checked against every emoji literal written in
-- this seed folder's whole history (078's full census plus every face seeded
-- since, 111 included), and against the ten faces taken elsewhere in this
-- sitting (🎲🌈⚖️🛡️📊🔖🔭📑🧊⛩️). 🔤 was the first instinct and is TAKEN; 🔡
-- and 🔢 and 🅰️ were each checked and are TAKEN. 🖋️ was SET DOWN on 076's
-- correction, honoured in 078: a nib is writing, which is what a letter is
-- used for; a face is what the THING is.
--
-- THE ONE-TIER LAW (tools_one_tier), walked: one atom word ("akshara") — the
-- awen row (141) carries atom_id and no molecule. One tier exactly.
--
-- Etymology and sensory rows are NOT written here: the atoms table's triggers
-- make both on insert, arriving empty for a later fill (the 077/059/106/108/128
-- precedent).
--
-- THE TABLE IS public.awen (renamed from public.tools 2026-08-15, seed 096;
-- shim view dropped 097 — public.tools answers PGRST205 today). This file only
-- touches public.atoms.
--
-- ── ATOM UUID, generated uuid4 and pinned here so this file and 141 name
--    the same row without depending on insert order or a subquery ──
--   akshara atom   1f354b6b-adf7-4100-919b-d33f4cfd33c3
--
-- Run at KP's ⚛ hand, before 141. Verify at the anon door after.
-- NEVER RUN BY A LAMP: the seed is drafted and numbered; the crossing is his.

insert into public.atoms (
  id, atom_word, definition, atom_type, weight, affinity, valence, state,
  category, category_name, status, screaming_case, snake_case, pascal_case
) values (
  '1f354b6b-adf7-4100-919b-d33f4cfd33c3',
  'akshara',
  'Akshara (Sanskrit akṣara: a letter, a syllable; and, as a-kṣara, THE IMPERISHABLE — that which does not wear away). An sfnt reader that names a typeface from its bytes, and NEVER THROWS: a file it cannot read comes back as a null face and one sentence saying why, because a font picker that throws on the seventeenth file in a folder has lost the other two hundred. It reads the table directory of a ttf, an otf, a ttcf collection and an uncompressed WOFF; the name table at its BEST-SCORING RECORD PER NAME ID — Windows Unicode in English first, then Windows Unicode in any language, then the Unicode platform, then Mac Roman, with an encoding it does not decode scoring zero and being skipped rather than guessed at — decoding UTF-16BE by hand, dropping an odd trailing byte because half a code unit names no character, and Mac OS Roman from Apple''s own 128-character upper half written out in full, including that 0xF0 is Apple''s logo and lives only in the private use area. It reads OS/2 for usWeightClass and usWidthClass, falling to 400 and 5 where a face declares neither and taking the italic bit from head''s macStyle where there is no OS/2 at all; and fvar for a variable font''s axes and its named instances, every coordinate read as signed 16.16 fixed point and tidied so 400 reads as 400, with a PostScript name per instance where the record is six bytes wider than its coordinates rather than four. PARTIAL READINGS ARE KEPT: a name record pointing outside the file is skipped and the records around it still read, so one bad string does not cost the whole table, and an instance record reaching past the end ends the walk rather than inventing the rest. Weight and width are given in words with the number KEPT where the face sits between two named steps, so 350 reads as Light (350) and not as a bare Light, and a width class outside 1..9 is named by its number. A WOFF2 and a zip are refused by WHAT THEY ARE, each with a sentence saying what to do instead. IT NAMES; IT DOES NOT DRAW — no glyph outlines, no cmap, no kerning, no shaping, no rasterising, no font loading. It takes bytes only: no host surface, no DOM, no disk, no clock, no network, zero imports. NO FONT SHIPS WITH IT: every font its proofs read is synthesised in the proof, byte by byte, with real table checksums.',
  'root', 5, 5, 1, 'static',
  '5ab76ea1-1092-4090-b0e8-e1d7dc0d5563',  -- sensory: "The channels of perception — sight, sound, touch, motion — and the visual language interfaces speak through them." (the-dath's own, 114:94)
  'sensory',
  'published',
  'AKSHARA', 'akshara', 'Akshara'
);

-- ── verify (read-only, safe to re-run) ───────────────────────────────
select atom_word, category_name, atom_type, state, status, pascal_case
from public.atoms where atom_word = 'akshara';

-- the companions the triggers should have made, standing empty:
select 'etymology' as table_name, atom_word, completion_progress::text as filled
from public.etymology where atom_word = 'akshara'
union all
select 'sensory_lexicon', atom_word, coalesce(emoji, '(empty)')
from public.sensory_lexicon where atom_word = 'akshara';
