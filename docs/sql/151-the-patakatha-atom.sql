-- 151 — the-patakatha, its atom
--
-- THE ORDER THIS FILE KEEPS: atom seed BEFORE awen row, and as its own
-- numbered file, in 077-the-promenade-atom.sql's exact shape and 147's
-- (the-procession) most recent reading of it. The awen row stands as
-- 152-the-patakatha-seeded.sql and runs after this one.
--
-- THE PATAKATHA — पटकथा (paṭakathā), Sanskrit: A SCREENPLAY, literally THE
-- SCREEN'S STORY. The water half of the screenplay format, born in the spring
-- at KP's ⚛ word, verbatim:
--   "we have a need for it to be repurposed as a book, manuscript, article,
--    all the reasons an author might publish. all types formatting assistance"
--   (resonance-chamber/desk/THE-AUTHORS-STUDIO.md:4-6)
--
-- A screenplay format is a format any realm might set, so it is born in the
-- spring and resonance-scribe mirrors it. NEITHER IMPORTS THE OTHER.
--
-- THE FINDING THAT MADE IT A WATER AND NOT A ROOM: the spring was swept by
-- name, case-insensitively, across the 87 tool folders beside this one for
-- screenplay, slugline, Fountain, Courier and teleplay. Hits stand in exactly
-- five folders and not one of them is a screenplay format: a code font stack
-- in the-binder (src/index.ts:1454) and in the-pandulipi (first at
-- src/index.ts:196), a Welsh gloss in the-ffynnon (first at README.md:11), two
-- emoji names in the-emoji-collector (emojis.gen.ts:2951, :3366) and a courier
-- in the-procession (README.md:129). The spring had the-binder, which
-- makes a book; the-pandulipi, which sets a submission; the-scrolls, which
-- renders a screen; and the-kinema, which knows motion. It had no screenplay
-- format at all. That absence ships in the build as the constant
-- NOTHING_STOOD, so the claim carries its own reason.
--
-- THE NAME IS RULED: the-patakatha. पटकथा joins the Sanskrit stratum beside
-- the-pandulipi, and it lives in exactly two places in the build: the constant
-- TOOL_NAME and the folder. A rename is one edit, one `git mv`, and one update
-- to this row and 152's.
--
-- THE BUILD AS IT STANDS, so this row can be checked against a file:
--   resonance-awen/tools/the-patakatha/src/index.ts — 806 lines,
--     sha256 9c00ba6c0df130117e978bc23c7fb70984af7854c3856aa3d73313a32e177d52
--   resonance-awen/tools/the-patakatha/verify.mjs — 424 lines,
--     sha256 bc224232099771ec87123680eee00a224ab0c2c4cf2d3ed73cb734debd9302ab
--   gates: `npm run check` silent · `npm run build` clean · 62/62 TRUE.
--
-- ── CATEGORY ─────────────────────────────────────────────────────────
-- **content** (420f7c5c-badc-4b75-9e30-d36b47bd11b7).
--
-- THE UUID IS THE ONE 144 AND 147 CARRY. This builder holds no key to the base
-- and ran no query of its own; the value was read from those two files, where
-- its provenance stands (144's header: confirmed live through the bridge by
-- the conductor; 147's: read from the 2026-08-24 grammar export).
--
-- WHY CONTENT: the category's own definition (024-the-category-faces.sql:74-76)
-- is "The stuff itself — documents, fragments, lyrics, recordings. What the
-- house holds, as distinct from how." A paṭakathā IS the stuff itself: the
-- script, before anything has been shot.
--
-- RUNNER-UP, AND THEIRS TO OVERRULE IN ONE WORD: **creation**
-- (8427a8ea-…) — because a screenplay is the thing a film is made FROM. It
-- was set down on 076's correction, honoured: a face, and a category, is what
-- the THING is, never what it is used for. This water makes no new writing; it
-- lays writing that already exists onto the format's own measure.
--
-- MOLECULE / ORGANISM — checked, and neither applies. `patakatha` is ONE WORD,
-- and the house's tools_one_tier constraint (seed 014) lets an awen row cross
-- at exactly one tier — the tier its own name lives at (055's law). A one-word
-- name crosses at the ATOM tier; there is no molecule row to make and no
-- organism above it, and adding one while also setting atom_id would violate
-- the constraint outright. The-promenade, the-sphragis, the-merismos, the-
-- stereos, the-pandulipi and the-procession all stand exactly this way:
-- atom_id set, molecule_id and organism_id null.
--
-- SHAPE TAKEN FROM THE-PANDULIPI'S OWN ROW (144/145, the water this one stands
-- beside): folksonomy_type `Awen` · status `imagined` at birth · `home` filled
-- in 152 (the 105/106 precedent). tool_type is `transform`: a work and its
-- parts in, a laid script out. Runner-up `emit`, and defensible — it hands
-- back a document — set down because what it does to the text is a change of
-- FORM and nothing else, which is transform's own word. The house's verb marks
-- read from the live atoms into
-- resonance-awen/src/lib/data/type-lexicon.json give transform 🦋.
--
-- FACE: 📽️ — a film projector: the screen's own instrument, for the water
-- named the screen's story. It reads against 078's rules: one glyph, no skin
-- tone, no ZWJ sequence, no flag, no red-alarm mark.
--
-- THE FACE SWEEP, and what it set down: 🎬 IS TAKEN — the-scene-loader wears
-- it (078-the-tool-faces.sql:154), and a beacon draft gives it to
-- resonance-sceal besides. 🎞️ is the-kinema's (133:84). Every page glyph is
-- worn: 📃 the-pandulipi (145), 📄 the CONTENT CATEGORY itself (024:74), 📜
-- the-scrolls (078:134), 📘 the-binder (078:156), and 📝 · 📑 · 📔 · 📖
-- elsewhere in 078's census. 📽️ and 🎥 are BOTH unworn across every
-- icon_emoji written in this seed folder's whole history (078's 79 face lines,
-- 111's two, the sixteen seeded 2026-09-02, the merismos's 🪙, the kinema's
-- 🎞️, the pandulipi's 📃 and the procession's). 🎥 was set down: a camera is
-- the shooting, and this water is the script. THE LIVE BASE MAY HOLD FACES
-- THIS HAND COULD NOT SEE — it holds no key and read no row — so 152 carries a
-- read-only check for a second wearer, and the glyph is theirs ⚛ to re-rule in
-- one word.
--
-- THE ONE-TIER LAW (tools_one_tier), walked: one atom word ("patakatha") — the
-- awen row (152) carries atom_id and no molecule. One tier exactly.
--
-- Etymology and sensory rows are NOT written here: the atoms table's triggers
-- make both on insert, arriving empty for a later fill (the 077/059/106/108/128
-- /144 precedent).
--
-- THE TABLE IS public.awen (renamed from public.tools 2026-08-15, seed 096;
-- shim view dropped 097 — public.tools answers PGRST205 today). This file only
-- touches public.atoms.
--
-- ── ATOM UUID, generated uuid4 and pinned here so this file and 152 name
--    the same row without depending on insert order or a subquery ──
--   patakatha atom   86bd440b-e9ee-4caf-ae34-431cb2d6c792
--
-- Run at KP's ⚛ hand, before 152. Verify at the anon door after.
-- NEVER RUN BY A LAMP: the seed is drafted and numbered; the crossing is theirs.

insert into public.atoms (
  id, atom_word, definition, atom_type, weight, affinity, valence, state,
  category, category_name, status, screaming_case, snake_case, pascal_case
) values (
  '86bd440b-e9ee-4caf-ae34-431cb2d6c792',
  'patakatha',
  'Patakatha (Sanskrit पटकथा: a screenplay — literally the screen''s story, paṭa the screen and kathā the tale). The script as the trade sets it, and nothing beyond that: a FIXED-PITCH page, which is the whole of it — twelve-point Courier at ten characters and six lines to the inch, on US Letter with margins of one inch and one and a half at the inner edge, giving SIXTY COLUMNS BY FIFTY-FOUR LINES exactly, with one page running one minute. Six elements and no more, each told apart by a stated test rather than a guess: a slugline is a shouted line opening INT., EXT., EST. or I/E.; a transition is a shouted line ending TO:, IN: or OUT:, or FADE OUT. read whole; a character cue is a shouted line no longer than the name column, not ending in a colon, and answered by a non-blank line; what follows a cue is dialogue until a blank line ends the speech, and a bracketed line inside it is a parenthetical; everything else is action. Their columns are arithmetic on the measure: action and slugline at column 0 across 60, dialogue at 10 across 35, a parenthetical at 16 across 20, a cue at 22 across 33, and a transition flush right at column 60. Two blank lines before a scene heading, one before action, a cue and a transition, none before a parenthetical or a line of speech; a blank line never opens a page; a slugline never stands as a page''s last line and a cue never as its last two. THE TEXT IS NEVER ALTERED, and that is the whole carefulness of it: UPPER CASE IS HOW THE GRAMMAR READS A LINE AND NEVER HOW IT SETS ONE, so a lower-case scene heading stays lower-case and is simply not a scene heading; wrapping breaks only at spaces, so a word longer than its column stands alone and overruns the column rather than being cut; every element carries its byte offsets in the source it was read from and is a byte-exact slice of it; and every character no element covers is one of a declared separator alphabet, so a dropped line would stand in a gap as its own letters. THE PAGE COUNT IS THE FORMAT''S OWN AND IS COUNTED HERE — a fixed-pitch page is arithmetic, not typesetting — which is exactly where this water parts from the-pandulipi, whose paged HTML only a print engine can measure. No (MORE) and no (CONT''D) is written; no scene numbers, no revision marks, no dual dialogue, no title-page key block, and no A4 measure invented. No clock, no disk, no network, no host global, zero imports: a work in, a laid script out, and nothing throws.',
  'root', 5, 5, 1, 'static',
  '420f7c5c-badc-4b75-9e30-d36b47bd11b7',  -- content: "The stuff itself — documents, fragments, lyrics, recordings. What the house holds, as distinct from how."
  'content',
  'published',
  'PATAKATHA', 'patakatha', 'Patakatha'
);

-- ── verify (read-only, safe to re-run) ───────────────────────────────
select atom_word, category_name, atom_type, state, status, pascal_case
from public.atoms where atom_word = 'patakatha';

-- the companions the triggers should have made, standing empty:
select 'etymology' as table_name, atom_word, completion_progress::text as filled
from public.etymology where atom_word = 'patakatha'
union all
select 'sensory_lexicon', atom_word, coalesce(emoji, '(empty)')
from public.sensory_lexicon where atom_word = 'patakatha';
