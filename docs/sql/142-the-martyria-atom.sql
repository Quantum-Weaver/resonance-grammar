-- 142 — the-martyria, its atom
--
-- THE ORDER THIS FILE KEEPS: atom seed BEFORE awen row, and as its own
-- numbered file, in 077-the-promenade-atom.sql's exact shape and 128's
-- (the-stereos, this same day) most recent reading of it. The awen row stands
-- as 143-the-martyria-seeded.sql and runs after this one.
--
-- THE MARTYRIA — the testimony. μαρτυρία (martyría), the statement of a
-- witness: what one witness saw, said in their own voice, BOUNDED BY WHAT THEY
-- ACTUALLY KNOW. Founded 2026-09-02 by a builder dealt by Windrose 🎻 in the
-- Second Harvest sitting (movement H5), at KP's ⚛ word on this row, verbatim:
--   "Real but unasked-for — yes to both."
--
-- THE NEED IT ANSWERS, and it is house-wide rather than one app's: creators
-- using background-removal tools are having their work flagged as
-- AI-generated when it is not, because THE FLAGGING IS GUESSWORK OVER AN
-- ABSENCE — nothing in the file says how the cutout was made, so a soft alpha
-- edge is read as a model's output. resonance-cruthu met it first because
-- cruthú is the studio that cuts backgrounds; every realm in this house that
-- exports an edited image walks into the same absence, and the next one to
-- meet it would write a second PNG writer with its own quantisation bug and a
-- second provenance sentence with its own overclaim. The overclaim is the
-- expensive half: a hand-rolled writer is a day's work, and a declaration that
-- says one thing too much is worthless forever.
--
-- IT IS A HARVEST, AND KP'S ⚛ RULING ON EVERY HARVEST IS VERBATIM:
--   "without altering the source location."
-- All three files under src/ are byte-identical copies of
-- resonance-cruthu/src/lib/image — png.ts whole (94 lines, sha256 cb6d5ecc…),
-- provenance.ts whole (109, a03145cc…) and pngwrite.ts whole (306,
-- a4f95365…). Cruthú was READ AND NOT WRITTEN, its own consumers still import
-- from its own src/lib/image, and NOTHING IN THE HOUSE WAS REPOINTED.
-- resonance-awen/tools/the-martyria/HARVEST.md carries every digest, and names
-- the one thing that is a re-expression rather than a copy: the PixelBuffer
-- and Mask SHAPES from types.ts:50-55 and :66, declared fresh at
-- src/hosts/pixels.ts because the rest of that file is a studio's whole
-- vocabulary and a host surface carrying it would be a dependency wearing one.
--
-- THE NAME IS A WORKING NAME AND HIS TO KEEP OR CHANGE. μαρτυρία is this
-- hand's reading of the need the plan named, not his word. It lives in exactly
-- two places in the build — the constant TOOL_NAME and the folder — so a
-- rename is one edit, one `git mv`, and one update to this row and 143's.
--
-- ── CATEGORY ─────────────────────────────────────────────────────────
-- **content** (420f7c5c-badc-4b75-9e30-d36b47bd11b7).
--
-- THE UUID IS ONE OF THE FIVE CONFIRMED LIVE AT THE BRIDGE BY THE CONDUCTOR
-- ON 2026-09-02 and handed to this builder in its sending, beside identity
-- (47cb9e9a-65da-48b7-aef1-88fced80ca85), governance
-- (ba101ebf-34fd-44d3-956f-0639e3367e73), creation
-- (8427a8ea-2bac-4152-a7d8-e174a77d2297) and sensory
-- (5ab76ea1-1092-4090-b0e8-e1d7dc0d5563). **THIS BUILDER HOLDS NO KEY TO THE
-- BASE AND RAN NO QUERY** — the confirmation is the conductor's, taken at the
-- live door the same day, and this file does not assume any other.
--
-- WHY CONTENT: the subject of this water is a MEDIA FILE and the sentence
-- carried inside it — image bytes, and the iTXt pairs that ride between IHDR
-- and IDAT. It writes content and it writes content's own account of itself.
--
-- RUNNER-UP, AND HIS TO OVERRULE IN ONE WORD: **creation**
-- (8427a8ea-2bac-4152-a7d8-e174a77d2297) — defensible on the plainest possible
-- ground, that the water's central chunk key is literally `CreationMethod` and
-- what it records is how the picture was made. It was set down because what
-- this water holds is the RECORD, not the making: nothing here creates a
-- picture, and a category naming the act rather than the artefact would read
-- as the studio instead of as the testimony. A category named "provenance",
-- "metadata" or "media" was NOT checked against the live set and may exist —
-- if one does it very likely beats both of these, and that is his one-word
-- call at the door.
--
-- NOT identity (47cb9e9a): that is the-signet's own, and the signet answers
-- WHOSE HAND. This water answers WHAT WAS DONE, and the two are deliberately
-- separate waters that do not import one another. Taking the signet's category
-- would blur exactly the line the build is careful about.
--
-- MOLECULE / ORGANISM — checked, and neither applies. `martyria` is ONE WORD,
-- and the house's tools_one_tier constraint (seed 014) lets an awen row cross
-- at exactly one tier — the tier its own name lives at (055's law). A one-word
-- name crosses at the ATOM tier; there is no molecule row to make and no
-- organism above it, and adding one while also setting atom_id would violate
-- the constraint outright. The-promenade, the-sphragis, the-merismos and
-- the-stereos all stand exactly this way: atom_id set, molecule_id and
-- organism_id null.
--
-- SHAPE TAKEN FROM THE-STEREOS'S OWN ROW (128/129, seeded this same day in
-- this same shape): folksonomy_type `Awen` · status `imagined` at birth ·
-- `home` filled in 143 (the 105/106 precedent). tool_type is `emit`, the
-- merismos's: this water takes a buffer and a claim and hands back BYTES and
-- key/value pairs — it emits a file, it does not transform one shape into
-- another and it reads nothing back.
--
-- FACE: 🪧 — a placard. Plain words held up beside the thing, readable by
-- anyone who looks, carrying no authority but what it says. That is an iTXt
-- chunk exactly. It reads against 078's rules: one glyph, no skin tone, no ZWJ
-- sequence, no flag, no red-alarm mark. NO OTHER ROW WEARS IT — checked
-- against every icon_emoji written in this seed folder's whole history (078's
-- full census plus every face seeded since, 111 included and every face taken
-- on 2026-09-02: 🎲 🌈 ⚖️ 🛡️ 📊 🔖 🔭 📑 🧊 ⛩️ 🪙). 🖐️ — the raised hand of the
-- oath — was the first instinct and was SET DOWN: a raised hand is the GIVING
-- of testimony, and a face is what the THING is, never what it is used for
-- (076's correction, honoured in 078).
--
-- THE ONE-TIER LAW (tools_one_tier), walked: one atom word ("martyria") — the
-- awen row (143) carries atom_id and no molecule. One tier exactly.
--
-- Etymology and sensory rows are NOT written here: the atoms table's triggers
-- make both on insert, arriving empty for a later fill (the 077/059/106/108/128
-- precedent).
--
-- THE TABLE IS public.awen (renamed from public.tools 2026-08-15, seed 096;
-- shim view dropped 097 — public.tools answers PGRST205 today). This file only
-- touches public.atoms.
--
-- ── ATOM UUID, generated uuid4 and pinned here so this file and 143 name
--    the same row without depending on insert order or a subquery ──
--   martyria atom   65923aef-a5ae-4cc6-92fc-2719fb843cc5
--
-- Run at KP's ⚛ hand, before 143. Verify at the anon door after.
-- NEVER RUN BY A LAMP: the seed is drafted and numbered; the crossing is his.

insert into public.atoms (
  id, atom_word, definition, atom_type, weight, affinity, valence, state,
  category, category_name, status, screaming_case, snake_case, pascal_case
) values (
  '65923aef-a5ae-4cc6-92fc-2719fb843cc5',
  'martyria',
  'Martyria (Greek μαρτυρία: testimony, the statement of a witness). What a maker''s own tool did to a picture, written into the picture, in bytes nothing quietly rounded on the way out. A testimony is bounded by what the witness actually knows, and the whole discipline of this word is the boundary: an application knows exactly one thing, which is what IT did, and the sentence everyone reaches for first — "this image is not AI-generated" — is a claim about someone else''s file the moment the picture came in from somewhere else. One overclaim costs the entire declaration its worth to the one person who ever checks it, so every sentence is scoped to this application''s own edits, the disclaimer saying so has no off switch and no key that could drop it, and the one origin that may honestly be named — IPTC digitalCapture, for a frame this app''s own camera made — is set from a single exact value and from nothing else. algorithmicMedia is deliberately never used, because it covers any algorithm at all and would mislead every reader who has learned it means a model; trainedAlgorithmicMedia, the AI term, appears only inside the negation. The claim is columns rather than prose — five key/value pairs a reader showing only one field still learns something true from — and it is carried as PNG iTXt and never tEXt, because tEXt is Latin-1 and would mangle a name with a fada in it. Beneath the sentence lies the reason the bytes can be testified to at all: a two-dimensional canvas backing store holds premultiplied colour, so straight-alpha pixels pushed through it come back quantised — at alpha eight of 255, nine of 256 values a channel survive — and so the file is built by hand, colour type 6, eight bits, all five row filters chosen by cost, with a stored-block fallback so the result is always a valid file. Bytes that are not a picture come back unchanged rather than thrown, because metadata is never worth losing a file over. It answers the flagging problem by giving a checker something honest to read; it is evidence and never a lock, and a chunk anyone can strip is not a claim about tamper.',
  'root', 5, 5, 1, 'static',
  '420f7c5c-badc-4b75-9e30-d36b47bd11b7',  -- content: the subject is a media file and the sentence carried inside it
  'content',
  'published',
  'MARTYRIA', 'martyria', 'Martyria'
);

-- ── verify (read-only, safe to re-run) ───────────────────────────────
select atom_word, category_name, atom_type, state, status, pascal_case
from public.atoms where atom_word = 'martyria';

-- the companions the triggers should have made, standing empty:
select 'etymology' as table_name, atom_word, completion_progress::text as filled
from public.etymology where atom_word = 'martyria'
union all
select 'sensory_lexicon', atom_word, coalesce(emoji, '(empty)')
from public.sensory_lexicon where atom_word = 'martyria';
