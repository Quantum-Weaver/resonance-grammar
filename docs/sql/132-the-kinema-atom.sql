-- 132 — the-kinema, its atom
--
-- THE ORDER THIS FILE KEEPS: atom seed BEFORE awen row, and as its own
-- numbered file, in 077-the-promenade-atom.sql's exact shape and 128's
-- (the-stereos) most recent reading of it. The awen row stands as
-- 133-the-kinema-seeded.sql and runs after this one.
--
-- THE KINEMA — motion. κίνημα (kínēma), the thing moved and the movement
-- itself, and the root the whole cinema is named from. Founded 2026-09-02 by a
-- builder dealt by Windrose 🎻 in THE SECOND HARVEST sitting, movement H1, at
-- KP's ⚛ word, verbatim: "the-kinema great name."
--
-- THE RULING IT SERVES — the second-harvest board, movement H1, and the
-- convergence that opened the row: the render road was forged TWICE, in the
-- same week, by two bodies that did not know of each other.
--   resonance-sceal/src/lib/render.ts (225) + renderer.ts (491)
--   resonance-ardan/src/lib/stage/film.ts (279)
-- Between them the same twenty-odd functions were written twice, and in SEVEN
-- places they were written differently. This water is the one road, with every
-- disagreement resolved out loud.
--
-- IT IS A HARVEST, AND KP'S ⚛ RULING ON EVERY HARVEST IS VERBATIM:
--   "without altering the source location."
-- No file under src/ is byte-identical to an origin — this is a
-- RE-EXPRESSION, not a copy, because two origins said the same thing two ways
-- and one had to be chosen. So the honesty is carried differently and more
-- expensively: BOTH origins' relevant functions are transcribed VERBATIM into
-- the water's own fixtures/ folder, and six of its twenty proofs feed both
-- sides the same inputs and compare the numbers.
-- resonance-awen/tools/the-kinema/HARVEST.md carries every origin digest,
-- every function's provenance by line, and every one of the seven divergences.
-- The three origin files were READ AND NOT WRITTEN. NOTHING WAS REPOINTED:
-- sceal keeps its render room, ardan keeps its film exporter, and both are
-- consumers-to-be rather than consumers.
--
-- THE NAME IS KP'S ⚛ OWN, and it lives in exactly two places in the build —
-- the constant TOOL_NAME and the folder — so a change of his mind is one edit,
-- one `git mv`, and one update to this row and 133's.
--
-- ── CATEGORY ─────────────────────────────────────────────────────────
-- **creation** (8427a8ea-2bac-4152-a7d8-e174a77d2297).
--
-- THE UUID IS FROM THE HOUSE'S LIVE CENSUS — **confirmed live 2026-09-02
-- through the bridge by the conductor**, along with the eight others standing
-- today: infrastructure 84af045b-c682-416d-b290-0fcd5ceaecd0 · creation
-- 8427a8ea-2bac-4152-a7d8-e174a77d2297 · content
-- 420f7c5c-badc-4b75-9e30-d36b47bd11b7 · sensory
-- 5ab76ea1-1092-4090-b0e8-e1d7dc0d5563 · data
-- fe347dea-adba-46f5-8c37-25e55202f484 · communication
-- 839cc74b-baee-47cc-b6eb-617a83393e36 · measurement
-- fa0d7a9e-439d-4545-b679-23a5f1487309 · knowledge
-- 339cb540-838f-407e-ab96-e433750ed9fb · identity
-- 47cb9e9a-65da-48b7-aef1-88fced80ca85. This builder holds no key to the base
-- and ran no query of its own.
--
-- WHY CREATION: what this water is for is the making of a new artefact. An
-- edit list goes in and ONE FILM comes out — a thing that did not exist
-- before, assembled from things that did. That is the creation family's whole
-- subject, and it is the category `cosmic` itself carries
-- (056-the-organism-and-the-music-hall.sql:33).
--
-- RUNNER-UP, AND HIS TO OVERRULE IN ONE WORD: **sensory**
-- (5ab76ea1-1092-4090-b0e8-e1d7dc0d5563), whose own definition in the live
-- base reads "The channels of perception — sight, sound, touch, MOTION — and
-- the visual language interfaces speak through them", and which therefore
-- names this water's own word out loud. It was set down because that category
-- is about the VOCABULARY of perception — what a body says it sees — while
-- this water is about ASSEMBLING a picture and a sound into a file. A face is
-- what the thing IS, and so is a category. But the argument is real, the
-- overlap is his to weigh, and it is written here rather than lost.
--
-- MOLECULE / ORGANISM — checked, and neither applies. `kinema` is ONE WORD,
-- and the house's tools_one_tier constraint (seed 014) lets an awen row cross
-- at exactly one tier — the tier its own name lives at (055's law). A one-word
-- name crosses at the ATOM tier; there is no molecule row to make and no
-- organism above it, and adding one while also setting atom_id would violate
-- the constraint outright. The-promenade, the-sphragis, the-merismos and
-- the-stereos all stand exactly this way: atom_id set, molecule_id and
-- organism_id null.
--
-- SHAPE TAKEN FROM THE-STEREOS'S OWN ROW (128/129, the most recent water
-- seeded in this shape): folksonomy_type `Awen` · status `imagined` at birth ·
-- `home` filled in 133 (the 105/106 precedent). tool_type is `transform`: an
-- edit list goes in and a plan comes back; a source size goes in and an even
-- frame size comes back; a probe goes in and a codec comes back. Nothing here
-- captures, arranges, witnesses or emits — it takes numbers and hands back
-- other numbers. The house's own verb marks read from the live atoms into
-- resonance-awen/src/lib/data/type-lexicon.json give transform 🦋.
--
-- FACE: 🎞️ — a strip of film frames, which is exactly and only what this
-- water is: a sequence of pictures on a grid in time. It reads against 078's
-- rules: one glyph, no skin tone, no ZWJ sequence, no flag, no red-alarm mark.
-- NO OTHER ROW WEARS IT — checked against every icon_emoji written in this
-- seed folder's whole history (078's full census of 122 faces plus every face
-- seeded since, 131 included), and against the ten faces taken TODAY by the
-- waters founded beside this one: 🎲🌈⚖️🛡️📊🔖🔭📑🧊⛩️.
-- 🎬 was the first instinct and was SET DOWN: a clapperboard is what a hand
-- does BEFORE a film — it marks a take, it is not the film — and it is already
-- worn in the census besides. 🎥 was set down and is UNWORN today: a camera is
-- a CAPTURE, and this water never touches a picture at all — it holds no pixel,
-- opens no encoder and paints no frame. It would have been a face for what the
-- water is used near, not for what it is. A face is what the THING is, never
-- what it is used for (076's
-- correction, honoured in 078).
--
-- ⚠ FOUR OTHER WATERS ARE BEING FOUNDED IN THIS SAME SITTING (movements
-- H3–H5). Their faces were not visible to this hand when 🎞️ was chosen. If two
-- seeds arrive at his door wearing one glyph, 078's test decides it: a shared
-- face must never share a tool_type, and the second one moves.
--
-- THE ONE-TIER LAW (tools_one_tier), walked: one atom word ("kinema") — the
-- awen row (133) carries atom_id and no molecule. One tier exactly.
--
-- Etymology and sensory rows are NOT written here: the atoms table's triggers
-- make both on insert, arriving empty for a later fill (the 077/059/106/108/128
-- precedent).
--
-- THE TABLE IS public.awen (renamed from public.tools 2026-08-15, seed 096;
-- shim view dropped 097 — public.tools answers PGRST205 today). This file only
-- touches public.atoms.
--
-- ── ATOM UUID, generated uuid4 and pinned here so this file and 133 name
--    the same row without depending on insert order or a subquery ──
--   kinema atom   c663f299-ac90-4e66-a02e-a1481a431ff5
--
-- Run at KP's ⚛ hand, before 133. Verify at the anon door after.
-- NEVER RUN BY A LAMP: the seed is drafted and numbered; the crossing is his.

insert into public.atoms (
  id, atom_word, definition, atom_type, weight, affinity, valence, state,
  category, category_name, status, screaming_case, snake_case, pascal_case
) values (
  'c663f299-ac90-4e66-a02e-a1481a431ff5',
  'kinema',
  'Kinema (Greek κίνημα: motion, the thing moved and the movement itself — the root the whole cinema is named from). Everything a render KNOWS about motion before a single frame is encoded, held as pure arithmetic: the frame times of a span at a FREE rate with 24 and 30 as named presets; the plan that deals every cut its frames by MASTER TIME, so a cut of one second at thirty owns exactly thirty frames, the frame standing on a boundary belongs to the NEXT cut, no frame is ever drawn twice or missed, and a cut of no length owns none; two ways of making a pixel size even, because the two bodies that forged this road rounded differently and both roundings are right about their own question; the codec walk as DATA — VP9 first, VP8 as the older fallback, and H.264 probed LAST and REFUSED BY NAME, because a machine offering only H.264 cannot write a WebM and a render must say so rather than mislabel the container; a take''s sound sliced to EXACTLY the samples a cut needs, padded with silence and never stretched, laid planar as an encoder wants it; many clips summed onto one mono line at their own starts and clamped rather than wrapped; back-pressure as a GAP and never a single threshold, so the decision to stop the frame source cannot chatter on the boundary; and the encoded chunks ordered by timestamp with the picture first on a tie. Beside the arithmetic stands a surface and never an implementation: four doors — a video encoder, an audio encoder, a muxer, and the frames themselves — declared and supplied by nobody here, so the engine that walks them can be proven in a room with no browser in it. No codec, no container, no canvas, no disk, no network, and NO CLOCK: a moment is six numbers a consumer declares. Numbers in, numbers out.',
  'root', 5, 5, 1, 'static',
  '8427a8ea-2bac-4152-a7d8-e174a77d2297',  -- creation: an edit list in, one film out — a thing that did not exist before, assembled from things that did. Cosmic's own category.
  'creation',
  'published',
  'KINEMA', 'kinema', 'Kinema'
);

-- ── verify (read-only, safe to re-run) ───────────────────────────────
select atom_word, category_name, atom_type, state, status, pascal_case
from public.atoms where atom_word = 'kinema';

-- the companions the triggers should have made, standing empty:
select 'etymology' as table_name, atom_word, completion_progress::text as filled
from public.etymology where atom_word = 'kinema'
union all
select 'sensory_lexicon', atom_word, coalesce(emoji, '(empty)')
from public.sensory_lexicon where atom_word = 'kinema';
