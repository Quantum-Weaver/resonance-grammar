-- 134 — the-kampyle, its atom
--
-- THE ORDER THIS FILE KEEPS: atom seed BEFORE awen row, and as its own
-- numbered file, in 077-the-promenade-atom.sql's exact shape and 128's
-- (the-stereos) most recent reading of it. The awen row stands as
-- 135-the-kampyle-seeded.sql and runs after this one.
--
-- THE KAMPYLE — the curve. καμπύλη (kampýlē), A CURVE; the feminine of
-- καμπύλος, "bent". Founded 2026-09-02 by a builder dealt by Windrose 🎻,
-- movement H3 of THE SECOND HARVEST
-- (resonance-chamber/desk/THE-SECOND-HARVEST.md), at KP's ⚛ word, verbatim:
--   "the easing solver, yes"
--
-- THE FINDING IT SERVES — the ardan scout's, quoted in the plan's row 44,
-- verbatim:
--   "the seam both sides of the theophany split ease through"
-- A house about to split in two held the arithmetic that makes motion agree
-- in BOTH halves inside ONE file in ONE half. The other half would have had
-- to copy it, and two copies agree only until somebody edits one.
--
-- IT IS A HARVEST, AND KP'S ⚛ RULING ON EVERY HARVEST IS VERBATIM:
--   "without altering the source location."
-- resonance-ardan/src/lib/stage/curve.ts (63 lines, sha256 5167d926…) was
-- READ AND NOT WRITTEN. It stands in the water byte-identical at
-- src/origin/curve.ts, excluded from the build, because its one import —
-- `easingOf` from `./score` — reaches into ardan's own easing table and
-- satisfying it would mean baking one house's curves into a spring water.
-- 1558 characters of it (the NAMED table, bezierOf, bez and bezierAt) are
-- carried into src/index.ts CHARACTER FOR CHARACTER, and the proofs read
-- both files off the disk at every run and check the carry rather than
-- assert it. ONE function was re-expressed and it is named: `ease`, which
-- now asks a declared host for the CSS string instead of reaching into a
-- score. resonance-awen/tools/the-kampyle/HARVEST.md carries the digest and
-- the road.
--
-- NOTHING WAS REPOINTED. ardan keeps curve.ts and keeps using it. KP ⚛
-- DECIDES WHICH REPO KEEPS curve.ts AFTER THE THEOPHANY SPLIT — the plan
-- says so in D1's own row — and this water only means the answer can now be
-- "neither".
--
-- THE NAME IS A WORKING NAME AND HIS TO KEEP OR CHANGE. καμπύλη is this
-- hand's reading of the need the plan named, not his word. It lives in
-- exactly two places in the build — the constant TOOL_NAME and the folder —
-- so a rename is one edit, one `git mv`, and one update to this row and
-- 135's.
--
-- ── CATEGORY ─────────────────────────────────────────────────────────
-- **infrastructure** (84af045b-c682-416d-b290-0fcd5ceaecd0).
--
-- THE UUID IS TAKEN FROM THE HOUSE'S OWN RECORDED CENSUS, NOT FROM A LIVE
-- QUERY — this builder holds no key to the base and ran none. Its addresses,
-- where the same uuid is already written down by a hand that DID read the
-- live table: 055-the-twenty-waters.sql:64 and :106, 061-the-cumdach.sql:13,
-- and 128-the-stereos-atom.sql, which records it as confirmed at the anon
-- door by KP's hand on 2026-09-02. **HIS TO CONFIRM AGAIN** before this file
-- is run; the seed does not assume it.
--
-- WHY INFRASTRUCTURE: the tether's and the spine's own category — "the
-- load-bearing line a body is built around; what everything else attaches
-- to." That is exactly what a shared easing seam is. Nothing looks at it;
-- every motion in both halves of a split bends through it, and the whole
-- reason it left ardan is that it is the thing two sides must agree on.
-- the-stereos took this category on 2026-09-02 for the same shape of
-- reason — load-bearing shared maths.
--
-- RUNNER-UP, AND HIS TO OVERRULE IN ONE WORD: **measurement**
-- (fa0d7a9e-439d-4545-b679-23a5f1487309) — a progress goes in and a number
-- comes out, which is measurement's shape. It was set down because what
-- comes out is not a reading of anything; it is a bend applied to a motion,
-- and the water's whole job is that two halves of a house bend the same way.
-- **sensory** (5ab76ea1-1092-4090-b0e8-e1d7dc0d5563) was also weighed and set
-- down: easing is FELT rather than seen, and a category named for the senses
-- would describe the effect rather than the thing. This builder read the
-- live 25 categories' definitions in no query — if one named "motion",
-- "mathematics" or "form" exists, it very likely beats all three, and that
-- is his one-word call at the door.
--
-- MOLECULE / ORGANISM — checked, and neither applies. `kampyle` is ONE WORD,
-- and the house's tools_one_tier constraint (seed 014) lets an awen row
-- cross at exactly one tier — the tier its own name lives at (055's law). A
-- one-word name crosses at the ATOM tier; there is no molecule row to make
-- and no organism above it, and adding one while also setting atom_id would
-- violate the constraint outright. The-promenade, the-sphragis, the-merismos
-- and the-stereos all stand exactly this way.
--
-- SHAPE TAKEN FROM THE-STEREOS'S OWN ROW (128/129, the most recent water
-- seeded in this shape): folksonomy_type `Awen` · status `imagined` at birth
-- · `home` filled in 135 (the 105/106 precedent). tool_type is `transform`:
-- a key and a progress go in, a number comes out. The house's own verb marks
-- read from the live atoms into resonance-awen/src/lib/data/type-lexicon.json
-- give transform 🦋.
--
-- state `static` — 048-the-meeting-atom.sql:14-15 verbatim: "active is verbs
-- and doers … static is nouns and things." A curve is a thing.
--
-- FACE: ➰ — a curly loop; a drawn bend, and nothing else. It reads against
-- 078's rules: one glyph, no skin tone, no ZWJ sequence, no flag, no
-- red-alarm mark, no variation selector needed. NO OTHER ROW WEARS IT —
-- checked against every `icon_emoji` written in this seed folder's whole
-- history (078's full census, 111, and every face seeded since, 128/129
-- included), which is the last full census written down; and checked against
-- the ten faces taken elsewhere in this same sitting
-- (🎲🌈⚖️🛡️📊🔖🔭📑🧊⛩️), which are all already in that census.
-- SET DOWN, and why: 🌀 — a cyclone is weather, and a spiral is not a bend.
-- 📈 — a chart is measurement's water and a face is what the THING is, never
-- what it is used for (076's correction, honoured in 078). 🎢 — a roller
-- coaster is the most legible easing curve in the whole emoji set and is a
-- RIDE, which is a use.
--
-- THE ONE-TIER LAW (tools_one_tier), walked: one atom word ("kampyle") — the
-- awen row (135) carries atom_id and no molecule. One tier exactly.
--
-- Etymology and sensory rows are NOT written here: the atoms table's
-- triggers make both on insert, arriving empty for a later fill (the
-- 077/059/106/108/128 precedent).
--
-- THE TABLE IS public.awen (renamed from public.tools 2026-08-15, seed 096;
-- shim view dropped 097 — public.tools answers PGRST205 today). This file
-- only touches public.atoms.
--
-- ── ATOM UUID, generated uuid4 and pinned here so this file and 135 name
--    the same row without depending on insert order or a subquery ──
--   kampyle atom   ded402bc-87e6-4494-8b1e-ab72fb16e5ef
--
-- Run at KP's ⚛ hand, before 135. Verify at the anon door after.
-- NEVER RUN BY A LAMP: the seed is drafted and numbered; the crossing is his.

insert into public.atoms (
  id, atom_word, definition, atom_type, weight, affinity, valence, state,
  category, category_name, status, screaming_case, snake_case, pascal_case
) values (
  'ded402bc-87e6-4494-8b1e-ab72fb16e5ef',
  'kampyle',
  'Kampyle (Greek καμπύλη: a curve, the feminine of καμπύλος "bent"). The bend of a motion, read as a number. A design system declares its curves once as CSS strings — linear, ease-in-out, cubic-bezier(0.4, 0, 0.2, 1) — and the browser reads those for its own animations; this reads the same strings for the three places the browser is not animating: a pose derived on a stage, a frame of a film rendered with no document in the room, a value tweened where no browser stands at all. A timing string is read as four bezier numbers, the parameter is solved for x by forty steps of bisection with an early break at 1e-6, and the answer is read on y. The whole point is that a transition and its own motion under the same key bend THE SAME WAY, in both halves of a house about to be split in two. IT HOLDS NO TABLE: which curve a key means belongs to whoever declares it, and the one host surface is a single verb — give me the CSS string for this key — whose null answer is an honest one. AN UNKNOWN KEY IS A REFUSAL AS DATA, never a throw and never a silent straight line: four ways to fail, four plain sentences, no exception anywhere. An overshooting curve is TOLD APART rather than clamped, because a spring that anticipates below zero and overshoots above one is doing what a spring does, and flattening one quietly is how motion dies. Progress outside 0..1 is brought in rather than extrapolated. Zero imports, no DOM, no disk, no clock: a key and a number in, a number out, the same on every machine.',
  'root', 5, 5, 1, 'static',
  '84af045b-c682-416d-b290-0fcd5ceaecd0',  -- infrastructure: the tether's and the spine's own — "the load-bearing line a body is built around; what everything else attaches to."
  'infrastructure',
  'published',
  'KAMPYLE', 'kampyle', 'Kampyle'
);

-- ── verify (read-only, safe to re-run) ───────────────────────────────
select atom_word, category_name, atom_type, state, status, pascal_case
from public.atoms where atom_word = 'kampyle';

-- the companions the triggers should have made, standing empty:
select 'etymology' as table_name, atom_word, completion_progress::text as filled
from public.etymology where atom_word = 'kampyle'
union all
select 'sensory_lexicon', atom_word, coalesce(emoji, '(empty)')
from public.sensory_lexicon where atom_word = 'kampyle';
