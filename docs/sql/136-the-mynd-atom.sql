-- 136 — the-mynd, its atom
--
-- THE ORDER THIS FILE KEEPS: atom seed BEFORE awen row, and as its own
-- numbered file, in 077-the-promenade-atom.sql's exact shape and 128's
-- (the-stereos) most recent reading of it. The awen row stands as
-- 137-the-mynd-seeded.sql and runs after this one.
--
-- THE MYND — the picture. mynd (Old Norse), A PICTURE, an image, a likeness;
-- and in the same word "the shape a thing has", from *munda*, to aim at, to
-- have in view. Founded 2026-09-02 by a builder dealt by Windrose 🎻,
-- movement H3 of THE SECOND HARVEST
-- (resonance-chamber/desk/THE-SECOND-HARVEST.md), at KP's ⚛ word, verbatim:
--   "yes, the picture road"
--
-- THE NEED IT SERVES — the ardan scout's finding, as the plan carries it
-- (row 45): three realms each want a picture of a board and each was going
-- to write this. **skapa, cruthu and sceal.** None is repointed this
-- sitting.
--
-- IT IS A HARVEST, AND KP'S ⚛ RULING ON EVERY HARVEST IS VERBATIM:
--   "without altering the source location."
-- All three files under src/ are byte-identical copies of
-- resonance-ardan/src/lib/stage, which was READ AND NOT WRITTEN:
--   snapshot.ts  92 lines  sha256 517c1366…
--   picture.ts  166 lines  sha256 3b5f1444…
--   geometry.ts  65 lines  sha256 5961e836…
-- ALL THREE COULD BE KEPT WHOLE — the rarest outcome a harvest has —
-- because picture.ts's one import is `./geometry` and geometry.ts sits
-- beside it here exactly as it sits beside it in ardan, so the import
-- resolves UNCHANGED and not one character of any file needed altering.
-- resonance-awen/tools/the-mynd/HARVEST.md carries every digest, and the
-- proofs re-hash all three at every run against those exact digests.
--
-- NOTHING WAS REPOINTED. ardan keeps all three files and keeps using them
-- from its own stage and film roads. Whether it ever consumes this water
-- instead is KP's ⚛ call, the same standing question as curve.ts and
-- the-kampyle (134/135, this sitting).
--
-- THE ONE ADDITION EDITS NOTHING: SVG_PROPS (16 names) and HTML_PROPS (14)
-- are module-private constants inside the copy. They are TRANSCRIBED, never
-- moved, into the index as an exported default a consumer may WIDEN, and the
-- copy's walk is re-expressed with its property list taken as an argument.
-- Both ends are proven rather than asserted: one proof parses the two arrays
-- back out of src/snapshot.ts on disk and compares them name for name and
-- order for order; another runs the wrapper at its defaults against the
-- untouched original over the same stage and compares the results CHARACTER
-- FOR CHARACTER.
--
-- THE NAME IS A WORKING NAME AND HIS TO KEEP OR CHANGE. mynd is this hand's
-- reading of the need the plan named, not his word. It lives in exactly two
-- places in the build — the constant TOOL_NAME and the folder — so a rename
-- is one edit, one `git mv`, and one update to this row and 137's.
--
-- ── CATEGORY ─────────────────────────────────────────────────────────
-- **creation** (8427a8ea-2bac-4152-a7d8-e174a77d2297).
--
-- THE UUID IS TAKEN FROM THE HOUSE'S OWN RECORDED CENSUS, NOT FROM A LIVE
-- QUERY — this builder holds no key to the base and ran none. Its address,
-- where a hand that DID read the live table wrote it down:
-- 056-the-organism-and-the-music-hall.sql:33 (the `cosmic` atom's own
-- category), and again in 128-the-stereos-atom.sql, which names it as the
-- runner-up it weighed and set down. **HIS TO CONFIRM AT THE ANON DOOR**
-- before this file is run; the seed does not assume it.
--
-- WHY CREATION: this water MAKES A THING THAT DID NOT EXIST — a standalone
-- SVG document and a run of PNG bytes, derived from a moving board at one
-- millisecond and stored nowhere. Nothing about it describes, measures or
-- registers; it produces.
--
-- RUNNER-UP, AND HIS TO OVERRULE IN ONE WORD: **content**
-- (420f7c5c-badc-4b75-9e30-d36b47bd11b7) — a picture is content, plainly, and
-- if the live category means "the stuff a thing is made of" it likely beats
-- creation. It was set down because this water makes the picture rather than
-- holds it. **sensory** (5ab76ea1-1092-4090-b0e8-e1d7dc0d5563) was weighed
-- and set down too: a picture is seen, but the category would name the
-- effect rather than the thing. This builder read the live 25 categories'
-- definitions in no query; a category named "image", "media" or "export", if
-- one exists, very likely beats all three, and that is his one-word call at
-- the door.
--
-- MOLECULE / ORGANISM — checked, and neither applies. `mynd` is ONE WORD,
-- and the house's tools_one_tier constraint (seed 014) lets an awen row
-- cross at exactly one tier — the tier its own name lives at (055's law). A
-- one-word name crosses at the ATOM tier; there is no molecule row to make
-- and no organism above it, and adding one while also setting atom_id would
-- violate the constraint outright.
--
-- SHAPE TAKEN FROM THE-STEREOS'S OWN ROW (128/129, the most recent water
-- seeded in this shape): folksonomy_type `Awen` · status `imagined` at birth
-- · `home` filled in 137 (the 105/106 precedent). tool_type is `capture`,
-- not the stereos's `transform`: a live, moving thing is frozen as it stands
-- at one instant, which is what capture is. The house's own verb marks read
-- from the live atoms into resonance-awen/src/lib/data/type-lexicon.json
-- give capture 📸 — which is ALSO the reason 📸 is not the face; a verb mark
-- and a row's face are two different things and should not collide.
--
-- state `static` — 048-the-meeting-atom.sql:14-15 verbatim: "active is verbs
-- and doers … static is nouns and things." A picture is a thing.
--
-- FACE: 🏞️ — a framed picture. It reads against 078's rules: one glyph plus
-- the variation selector (the census's own 🗺️, 🎛️, 🏷️ and 🖼️ all carry one),
-- no skin tone, no ZWJ sequence, no flag, no red-alarm mark. NO OTHER ROW
-- WEARS IT — checked against every `icon_emoji` written in this seed
-- folder's whole history (078's full census, 111, and every face seeded
-- since, 128/129 included), and against the ten faces taken elsewhere in
-- this same sitting (🎲🌈⚖️🛡️📊🔖🔭📑🧊⛩️).
-- SET DOWN, and why: 🖼️ — the true one, and it is ALREADY WORN TWICE, by
-- the-art-finder and the-gallery (078). A third wearer of a doubled face
-- would make the switchboard unreadable, so it was set down. 📸 — a camera
-- is a device, and it is the verb mark `capture` already carries. 🖌️ — a
-- brush is painting, which this water is not.
--
-- THE ONE-TIER LAW (tools_one_tier), walked: one atom word ("mynd") — the
-- awen row (137) carries atom_id and no molecule. One tier exactly.
--
-- Etymology and sensory rows are NOT written here: the atoms table's
-- triggers make both on insert, arriving empty for a later fill (the
-- 077/059/106/108/128 precedent).
--
-- THE TABLE IS public.awen (renamed from public.tools 2026-08-15, seed 096;
-- shim view dropped 097). This file only touches public.atoms.
--
-- ── ATOM UUID, generated uuid4 and pinned here so this file and 137 name
--    the same row without depending on insert order or a subquery ──
--   mynd atom   5f8b0664-d3ac-4301-99ed-9a504ce5dd7b
--
-- Run at KP's ⚛ hand, before 137. Verify at the anon door after.
-- NEVER RUN BY A LAMP: the seed is drafted and numbered; the crossing is his.

insert into public.atoms (
  id, atom_word, definition, atom_type, weight, affinity, valence, state,
  category, category_name, status, screaming_case, snake_case, pascal_case
) values (
  '5f8b0664-d3ac-4301-99ed-9a504ce5dd7b',
  'mynd',
  'Mynd (Old Norse: a picture, an image, a likeness; and the shape a thing has, from munda, to aim at, to have in view). A living, animating drawing held still at one instant and handed over as a picture that stands on its own. A board is moving and somebody wants a picture of it, right now, exactly as it looks — not a re-render, not an approximation from the model, the actual thing. The browser already knows the answer: a paused animation''s transform is a matrix in computed style and its opacity is a number there, so the hard part is done, by the engine, for free, and all that is needed is to read it and write it down. The world group is cloned, the live tree and the clone are walked side by side, each element''s COMPUTED look and place are written inline on the clone, the class names the stylesheet gave meaning to are dropped, the reader''s own pan and zoom are taken off, the definitions the marks point at travel inside the same text, and the result is serialized. THE ORIGINAL IS READ AND NEVER WRITTEN. Then pure string work: the frozen markup wrapped as a standalone document at one unit to one pixel, byte-stable so the same instant gives the same characters forever; a data URL; and a paint onto a canvas that hands back the picture''s bytes. Every door to the drawing machine is INJECTED through a surface of two verbs, so the whole road runs where no browser stands and the proofs walk it there with a browser written by hand. The picture is DERIVED AND NEVER STORED — not the millisecond, not the size, not the pixels — and an empty board has no picture at all and says so, rather than answering with a blank rectangle. The pixel box rounds outward so no mark ever loses an edge to a rounding.',
  'root', 5, 5, 1, 'static',
  '8427a8ea-2bac-4152-a7d8-e174a77d2297',  -- creation: the `cosmic` atom's own category (056:33) — this water makes a thing that did not exist
  'creation',
  'published',
  'MYND', 'mynd', 'Mynd'
);

-- ── verify (read-only, safe to re-run) ───────────────────────────────
select atom_word, category_name, atom_type, state, status, pascal_case
from public.atoms where atom_word = 'mynd';

-- the companions the triggers should have made, standing empty:
select 'etymology' as table_name, atom_word, completion_progress::text as filled
from public.etymology where atom_word = 'mynd'
union all
select 'sensory_lexicon', atom_word, coalesce(emoji, '(empty)')
from public.sensory_lexicon where atom_word = 'mynd';
