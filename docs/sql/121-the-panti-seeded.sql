-- 121 — the-panti seeded
--
-- THE ORDER THE HOUSE KEEPS: the atom seed FIRST, as its own numbered file
-- (077-the-promenade-atom.sql's shape), then the awen row. 120 is the atom;
-- this is the row, and it references the uuid 120 pins explicitly rather than
-- looking it up by `select … where atom_word =`.
--
-- THE PANTI — the row. The first of four waters raised 2026-09-02 in movement
-- A2 of the Unnamed Waters sitting, by a builder dealt by Windrose 🎻 at KP's ⚛
-- word, verbatim: "hold the Daedalus work and proceed with the rest, we will do
-- it on its own."
--
-- THE BOARD ROW THIS SERVES — the Unnamed Waters board, ROW 39, carried through
-- resonance-chamber/desk/THE-UNNAMED-WATERS-SITTING.md:76:
--   "the-panti (Sanskrit paṅkti, a row) — the table logic, its one peer type
--    inlined. Row 39. Seeds 120/121."
--
-- THE HARVEST RULING, KP ⚛ verbatim (row 33, lede·5): "without altering the
-- source location." Honoured to the byte — see 120's header for the sha256 that
-- matches on both sides, and the folder's own HARVEST.md for the whole record.
--
-- THE NAME IS A WORKING NAME AND HIS TO KEEP OR CHANGE. It lives in exactly two
-- places in the build — the constant TOOL_NAME and the folder — so a rename is
-- one edit, one `git mv`, and one update to this row.
--
-- CATEGORY TAKEN FROM SEED 077'S RECORDED CENSUS; CONFIRMED AT THE ANON DOOR BY
-- KP'S HAND: **content** (420f7c5c-badc-4b75-9e30-d36b47bd11b7). See 120's
-- header for the reading and for the knowledge runner-up left there for KP to
-- overrule in one word.
--
-- SHAPE TAKEN FROM THE-MERISMOS'S OWN ROW (seed 110), which was itself read
-- from the live shelf: folksonomy_type `Awen` · status `imagined` at birth ·
-- `home` filled here (the 105/106/110 precedent). tool_type is `arrange` —
-- the verb marks are the Grammar's own (capture 📸 · combine 🥣 · arrange 🎼 ·
-- witness 🧿 · emit 📤 · transform 🦋 · translate 🌏, recorded in 078's header),
-- and sorting, filtering and choosing rows is arranging. The other three waters
-- of this movement take `capture` (the-lakshana), `witness` (the-drishti) and
-- `arrange` (the-suchi); the-suchi shares this verb and wears a different face,
-- which is exactly the test 078 requires.
--
-- FACE: 📊 — a table of rows. One glyph, no skin tone, no ZWJ, no flag, no
-- red-alarm mark. Absent from 078's census and from 105/106/110/111. THE
-- COLLISION TEST IS THE CONDUCTOR'S AT LANDING.
--
-- THE ONE-TIER LAW (tools_one_tier), walked: one atom word ("panti") — the row
-- carries atom_id and no molecule. One tier exactly. (120's header records why
-- no molecule or organism row exists.)
--
-- GATES AT LANDING: `npm install && npm run build && npm run check` silent,
-- `npm run prove` 12/12 TRUE over twenty real rows transcribed from
-- AudHDities/docs/SUPERPOSITION-TABLE-REVIEW.md:69-88.
--
-- Run at KP's ⚛ hand, after 120. Verify at the anon door after.
-- NEVER RUN BY A LAMP.
--
-- Local mirror pin (progenatrix): e54ca284-7593-4452-82b9-f3370bafc1cb

insert into public.awen
  (name, slug, tool_type, definition, home, status, icon_emoji, folksonomy_type, atom_id, story, created_by)
values
  ('The Panti', 'the-panti', 'arrange',
   'THE ROW — a table''s own logic as data, harvested whole and unedited from AudHDities'' runes shelf and given the proofs it never had. THE SORT CYCLE CLOSES: null to ascending to descending to null, so three clicks on a header return the order the rows arrived in; clicking a different column starts it ascending and the old column forgets, because this water never sorts by two columns at once and does not pretend to. AN ABSENT VALUE IS NOT A SMALL VALUE — null and undefined sort LAST in BOTH directions, the one asymmetry in the file and the right one: a row with no verdict is not the earliest verdict. SORTING IS A COPY, never a mutation, and every selection act returns a new Set, so a store or a second view holding the same array is never changed underneath it. Three filter modes and no fourth — contains (the default, written into the switch rather than assumed), exact, startsWith — case-folded on both sides, with a whitespace-only needle refused as no filter at all and a null cell matching nothing; many filters compound with AND, folded left. A ROW''S ID IS THE ROW''S OWN where the row has one, so it travels through a re-order and a selected row stays selected; where the data offers no identity the fallback is `row-<index>`, which is POSITIONAL, and the water says so in its README, its proofs and its limits rather than offering an illusion. The one type-position reference to a peer type — React.ReactNode — is DECLARED as a host surface and supplied by nothing, so the copy compiles unchanged: the-clavis''s law applied to a type instead of a machine. No DOM, no framework, no clock, no network, no disk, zero imports, and nothing throws on an empty table, a missing key or an unknown column.',
   'resonance-awen/tools/the-panti', 'imagined', '📊', 'Awen',
   '0afc75d7-3436-4c6b-8e0f-2670c2d9caa6',  -- the panti atom, seeded in 120
   'Built 2026-09-02 by a builder dealt by Windrose 🎻, at KP''s ⚛ word "hold the Daedalus work and proceed with the rest" — movement A2 of the Unnamed Waters sitting, four waters in one hand, this one first. HARVESTED, NOT WRITTEN: the logic is a year of somebody''s decisions about tables, already made and already right, sitting in a component shelf where nothing could prove them. The copy is byte-identical to its origin (9,317 bytes, 310 lines, sha256 ffd8fbbd…499c1 on both sides) and what this build added is proof and a home — 12 printing proofs over twenty real rows transcribed from AudHDities/docs/SUPERPOSITION-TABLE-REVIEW.md:69-88, the walk-list Fable 🎻 built 2026-07-28 the day the superposition base learned to know itself. THE FINDING THIS BUILD OWES THE RECORD: table.utils.ts has NO IMPORTER in AudHDities — grep returns only the file''s own first-line path comment. It was written ahead of its component. The sending assumed a consumer and asked the fixture to come from it; the honest answer was a real table from the same repo''s documents and a paragraph saying why, written into HARVEST.md, into verify.mjs''s own header, and into the README, so nobody infers a live dependency that does not exist. THE NAME IS A WORKING NAME AND HIS TO KEEP OR CHANGE — it lives in one constant and one folder so a rename is cheap.',
   'a builder dealt by Windrose 🎻, at KP''s ⚛ word');

-- THE CROSSING — his ⚛ word alone.
-- The standing ruling ("if a tool is working, it is flowing") argues for it now
-- that the proofs stand; uncomment at his word and run:
-- update public.awen set status = 'flowing' where slug = 'the-panti';

-- ── verify (read-only, safe to re-run) ───────────────────────────────
select name, slug, tool_type, status, icon_emoji, folksonomy_type, home
from public.awen where slug = 'the-panti';

select atom_word, category_name, atom_type, state, status, pascal_case
from public.atoms where atom_word = 'panti';

-- no other row should wear 📊 — and per 078's law, if one does it must at least
-- carry a DIFFERENT tool_type, or the two rows are indistinguishable wherever
-- both are shown:
select icon_emoji, count(*) as wearers, string_agg(name || ' (' || tool_type || ')', ' · ' order by name)
from public.awen where icon_emoji = '📊' group by icon_emoji;

-- the companions the triggers should have made, standing empty:
select 'etymology' as table_name, atom_word, completion_progress::text as filled
from public.etymology where atom_word = 'panti'
union all
select 'sensory_lexicon', atom_word, coalesce(emoji, '(empty)')
from public.sensory_lexicon where atom_word = 'panti';
