-- 113 — the-hlutr seeded
--
-- THE HLUTR — the lot cast, and the test that says the cast was honest. Built
-- 2026-09-02 by a builder dealt by Windrose 🎻 at KP's ⚛ word, verbatim: "hold
-- the Daedalus work and proceed with the rest, we will do it on its own."
-- Movement A1 of the Unnamed Waters sitting, and this row closes the board's
-- ROWS 34 AND 35 TOGETHER (resonance-chamber/desk/THE-UNNAMED-WATERS-SITTING.md:70).
--
-- THE HARVEST RULING — KP ⚛, verbatim, board row 33: "without altering the
-- source location." resonance-tarocchi/src/lib/random.ts and
-- src/lib/workings/chisq.ts were copied byte-verbatim into the water's src/;
-- the origin is untouched and still runs both. HARVEST.md in the folder
-- records the byte counts and the SHA256 of each copy, and verify.mjs proof 12
-- re-checks both digests on every run.
--
-- THE NAME IS A WORKING NAME AND HIS TO KEEP OR CHANGE. It lives in exactly
-- two places — the constant TOOL_NAME and the folder — so a rename is one
-- edit, one `git mv`, and one update to this row.
--
-- CATEGORY: **knowledge** (339cb540-838f-407e-ab96-e433750ed9fb) — category
-- taken from seed 080's recorded census; confirmed at the anon door by KP's
-- hand. See 112's header for the definition matched, and for the
-- `measurement` runner-up left there for him to overrule in one word, with
-- the honest reason it was not taken (its uuid is not recorded in seeds
-- 077–080 or 105–110, and this hand could not reach the live table).
--
-- TOOL_TYPE: **emit** — the water hands back a draw, and the draw is what a
-- caller reaches for; the χ² half witnesses it. RUNNER-UP AND HIS TO OVERRULE
-- IN ONE WORD: `witness`, defensible because judging a run is half the folder.
--
-- FACE: 🎲 — the die, the lot itself. One glyph, no skin tone, no ZWJ, no
-- flag, no red-alarm mark, and no other row wears it (checked against 078's
-- whole census and against the faces set since: 🗝️ 🔒 🪙). 078's own test
-- ("do the rows that share emojis have different tool type values?") does not
-- arise — this face is shared with nobody.
--
-- THE ONE-TIER LAW (tools_one_tier), walked: one atom word ("hlutr") — the row
-- carries atom_id and no molecule. One tier exactly. (112's header records why
-- no molecule or organism row exists.)
--
-- Etymology and sensory rows are NOT written here: the atoms table's triggers
-- make both on insert (112), arriving empty for a later fill.
--
-- THE TABLE IS public.awen. Renamed from public.tools 2026-08-15 (seed 096);
-- the shim view was dropped at KP's word on 097.
--
-- Local mirror pin (progenatrix): 1fcf6abf-6bd2-4fb1-b069-45992fd8d3e4
--
-- GATE AT LANDING, run from a clean shell before this file was written:
--   npm install && npm run build && npm run check && npm run prove
--   → check silent, 13/13 TRUE, deterministic (every draw seeded, no clock)
--
-- Run at KP's ⚛ hand, after 112. Verify at the anon door after.

insert into public.awen
  (name, slug, tool_type, definition, home, status, icon_emoji, folksonomy_type, atom_id, story, created_by)
values
  ('The Hlutr', 'the-hlutr', 'emit',
   'The lot cast, and the test that says the cast was honest — the board''s rows 34 and 35 as ONE water, because a generator with no test is an assertion and a test with no generator has nothing to read. THE MODULO IS NOT A REDUCTION: x mod n favours the low faces unless n divides 2^32, so the bounded draw computes limit = floor(2^32/n)*n and REJECTS the short tail rather than folding it back in; shown side by side with a folding twin on ONE seeded stream at 8-bit words, where the 43/256-against-42/256 tilt is caught at p = 1e-14 and the rejecting run passes — the same formula the copied file uses, at a size a reader can check by hand. Fisher-Yates draws its index over [0, i] inclusive and freshly each pass, and the proof is not "did the items survive" but "does one card land in every position equally often": 156,000 shuffles judged by chi-square, which is the test a whole-array shuffle fails while still returning a valid permutation every time. A DEAL IS WITHOUT REPLACEMENT — 1,500 deals across 300 seeds and five hand sizes with not one repeat; more than the pool holds returns the pool, a negative count returns nothing, and neither refusal throws. EVERY SOURCE NAMES ITSELF AND SAYS WHETHER IT REPLAYS, and the primitive is never implemented: the platform''s own CSPRNG is wrapped, the seeded generator says in its own name that it is not a substitute for it, and this water never chooses between them for a caller. The chi-square half reports degrees of freedom, the expected count and whether the run is RELIABLE at all, refuses to judge four faces on ten rolls, and names "suspiciously even — real randomness is lumpier than this" as loudly as "far more uneven than chance explains", because counts too even are exactly as unlikely as counts too ragged; a single low p proves nothing alone and the verdict says so in its own sentence. It refuses rather than returns a biased value — the one throw in the water, and it is the honest one. No clock, no disk, no network, zero imports; the single host global is crypto.getRandomValues, declared and never implemented.',
   'resonance-awen/tools/the-hlutr', 'imagined', '🎲', 'Awen',
   '5979f938-8487-48a4-a097-f53de0b307a4',  -- the hlutr atom, seeded in 112
   'Built 2026-09-02 by a builder dealt by Windrose 🎻, at KP''s ⚛ word "hold the Daedalus work and proceed with the rest, we will do it on its own" — movement A1 of the Unnamed Waters sitting, four waters from the harvests, this one the first. Rows 34 and 35 of the board were two rows and are one water. HARVESTED UNDER HIS OWN RULING, verbatim: "without altering the source location" — resonance-tarocchi/src/lib/random.ts (110 lines) and src/lib/workings/chisq.ts (105 lines) copied BYTE-VERBATIM, zero lines changed, the origin untouched and still running both, and the SHA256 of each copy pinned in the water''s own proofs and re-checked every run. THE FIXTURE IS THE ORIGIN''S OWN USAGE, TRANSCRIBED: tarocchi counts with src/lib/workings/tally.ts:20-34 and judges with chiSquareUniform at Prove.svelte:39, choosing its source at prove.worker.ts:22, and that loop is the only loop the proofs count with. Every draw in the proof file is SEEDED, so the run reads identically on every machine and in every year; the platform generator is touched exactly once, to read its name, and never to draw a number a proof depends on. Gate at landing from a clean shell: check silent, 13/13 TRUE. THE NAME IS A WORKING NAME AND HIS TO KEEP OR CHANGE — it lives in one constant and one folder so a rename is cheap.',
   'a builder dealt by Windrose 🎻, at KP''s ⚛ word');

-- THE CROSSING — his ⚛ word alone.
-- The standing ruling ("if a tool is working, it is flowing") argues for it
-- now that the proofs stand; uncomment at his word and run:
-- update public.awen set status = 'flowing' where slug = 'the-hlutr';

-- ── verify (read-only, safe to re-run) ───────────────────────────────
select name, slug, tool_type, status, icon_emoji, folksonomy_type, home
from public.awen where slug = 'the-hlutr';

select atom_word, category_name, atom_type, state, status, pascal_case
from public.atoms where atom_word = 'hlutr';

-- no other row should wear 🎲:
select icon_emoji, count(*) as wearers, string_agg(name, ' · ' order by name)
from public.awen where icon_emoji = '🎲' group by icon_emoji;

-- the companions the triggers should have made, standing empty:
select 'etymology' as table_name, atom_word, completion_progress::text as filled
from public.etymology where atom_word = 'hlutr'
union all
select 'sensory_lexicon', atom_word, coalesce(emoji, '(empty)')
from public.sensory_lexicon where atom_word = 'hlutr';
