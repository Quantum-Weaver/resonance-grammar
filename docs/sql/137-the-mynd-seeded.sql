-- 137 — the-mynd seeded
--
-- THE MYND — the picture. mynd (Old Norse), A PICTURE, an image, a likeness.
-- The house's picture road, standing as its own water. Founded 2026-09-02 by
-- a builder dealt by Windrose 🎻, movement H3 of THE SECOND HARVEST
-- (resonance-chamber/desk/THE-SECOND-HARVEST.md), at KP's ⚛ word, verbatim:
--   "yes, the picture road"
--
-- THE NEED IT SERVES — the ardan scout's finding, as the plan carries it
-- (row 45): **skapa, cruthu and sceal each want a picture of a board**, and
-- each was going to write this. NONE IS REPOINTED THIS SITTING; a water is
-- pointed at its consumers at KP's ⚛ word, never a builder's.
--
-- IT IS A HARVEST, AND KP'S ⚛ RULING ON EVERY HARVEST IS VERBATIM:
--   "without altering the source location."
-- All three files under src/ are byte-identical copies of
-- resonance-ardan/src/lib/stage — snapshot.ts (92, 517c1366…), picture.ts
-- (166, 3b5f1444…) and geometry.ts (65, 5961e836…) — read and never written.
-- ALL THREE COULD BE KEPT WHOLE because picture.ts's one import, `./geometry`,
-- resolves here unchanged. NOTHING WAS REPOINTED: ardan keeps all three and
-- keeps using them.
--
-- A FINDING AT THE GATE, REPORTED AND NOT ACTED ON: at the copy,
-- `git -C resonance-ardan status --porcelain` was empty; at the gate it was
-- not, because D1 is editing ardan in the same sitting. NONE of the three
-- files harvested here is among D1's changes, by the plan and in fact, and
-- every digest was taken twice and agreed both times.
--
-- CATEGORY: **creation** (8427a8ea-2bac-4152-a7d8-e174a77d2297) — see 136's
-- header for the uuid's address in the house's recorded census (056:33, and
-- 128 which weighed it), for why creation and not content or sensory, and
-- for the plain statement that this builder ran no live query. **HIS TO
-- CONFIRM AT THE ANON DOOR** before this file is run.
--
-- SHAPE TAKEN FROM THE-STEREOS'S OWN ROW (128/129): folksonomy_type `Awen` ·
-- status `imagined` at birth · `home` filled here (the 105/106 precedent).
-- tool_type is `capture` — a live, moving thing frozen as it stands.
--
-- FACE: 🏞️ — a framed picture. One glyph plus the variation selector (078's
-- own 🗺️, 🎛️, 🏷️ and 🖼️ all carry one), no skin tone, no ZWJ, no flag, no
-- red-alarm mark, and no other row wears it (checked against 078's whole
-- census, 111, every face seeded since including 128/129, and the ten faces
-- taken elsewhere in this same sitting). 🖼️ is the true one and is ALREADY
-- WORN TWICE — the-art-finder and the-gallery, both in 078 — so it was set
-- down rather than trebled. 📸 was set down: a camera is a device, and 📸 is
-- already the verb mark this row's own tool_type `capture` carries.
--
-- THE ONE-TIER LAW (tools_one_tier), walked: one atom word ("mynd") — the
-- row carries atom_id and no molecule. One tier exactly. (136's header
-- records why no molecule or organism row exists.)
--
-- Etymology and sensory rows are NOT written here: the atoms table's
-- triggers make both on insert, arriving empty for a later fill.
--
-- THE TABLE IS public.awen. It was renamed from public.tools on 2026-08-15
-- (seed 096) and the shim view was dropped at KP's word on 097 —
-- public.tools answers PGRST205 today.
--
-- Local mirror pin (progenatrix): b6a30866-cdda-410c-aaea-98a5c613c7d5
--   (pinned by the conductor, Windrose, 2026-09-02 — the builder's hand had no grant on the base and said so below.)
-- No local mirror pin is written here. This builder is forbidden the base by
-- its own sending and read none; the progenatrix row is the conductor's.
--
-- Run at KP's ⚛ hand, after 136. Verify at the anon door after.
-- NEVER RUN BY A LAMP: the seed is drafted and numbered; the crossing is his.

insert into public.awen
  (name, slug, tool_type, definition, home, status, icon_emoji, folksonomy_type, atom_id, story, created_by)
values
  ('The Mynd', 'the-mynd', 'capture',
   'THE PICTURE ROAD, standing as its own water. A live, animating SVG frozen into standalone text at one instant, and that text painted to PNG bytes. The browser already holds the answer to "where is everything right now" — a paused Web Animation''s matrix and opacity are sitting in computed style — so the freeze clones the world group, walks the live tree and the clone in step, writes each element''s COMPUTED style inline on the clone, drops the class names the stylesheet gave meaning to, takes off the reader''s own pan and zoom, carries the <defs> the marks point at, and serializes. THE DOM IS READ AND NEVER WRITTEN, and the proofs check that the live tree still carries every class and its pan-and-zoom transform after a freeze has run over it. Then pure string work: the frozen markup wrapped as a standalone SVG document at 1:1 in board units, BYTE-STABLE — the same snapshot gives the same characters, every run — a data URL that round-trips exactly, and a paint onto a canvas that hands back PNG bytes. EVERY BROWSER DOOR IS INJECTED through a declared PictureDom of two verbs, so the whole road runs in Node against a stub and the proofs do exactly that with a browser written by hand in ninety lines and no dependency of any kind. THE PICTURE IS DERIVED AND NEVER STORED — not the millisecond, not the size, not the pixels — and an empty board has NO picture and says so with null rather than a 0×0 canvas. The pixel box rounds its origin down and its size up so no mark loses an edge. A HARVEST AND NEVER A RE-POINT: snapshot.ts, picture.ts and geometry.ts are byte-identical copies of resonance-ardan/src/lib/stage, all three kept WHOLE because picture.ts''s one import resolves here unchanged, re-hashed at every proof run against the digests recorded at the copy. THE ONE ADDITION EDITS NOTHING: the copy''s private property lists become an exported default a consumer may WIDEN, transcribed and proven against the file on disk, with the wrapper at its defaults proven character-identical to the untouched original. Zero runtime dependencies; no DOM lib and no @types — the browser names it uses are declared in one file and nowhere else. It throws in exactly four places and all four are named in the README.',
   'resonance-awen/tools/the-mynd', 'imagined', '🏞️', 'Awen',
   '5f8b0664-d3ac-4301-99ed-9a504ce5dd7b',  -- the mynd atom, seeded in 136
   'Founded 2026-09-02 by a builder dealt by Windrose in THE SECOND HARVEST, movement H3, at KP''s ⚛ word "yes, the picture road", answering the scout''s finding that skapa, cruthu and sceal each want a picture of a board and each was going to write this. Ardán wrote it first and wrote it well — well enough that the scout did not have to argue for it: the file already injected its browser doors, and the pure half was already separated from the DOM half, in the origin''s own words at the top of snapshot.ts, "so the proofs can walk the pure road alone." What it did not have was a reason to be in ardan rather than anywhere; a frozen SVG is not a board thing, it is a browser thing, and the browser is the same one in all three realms. So all three files came whole — the rarest outcome a harvest has — because picture.ts''s one import was ./geometry and geometry.ts came too, and not one character needed altering. geometry.ts brought its other five point formulas along and they are re-exported rather than hidden, because trimming a verbatim copy makes it something other than a copy. The only thing that had to be ADDED was a way to widen two module-private property lists without touching the file that holds them, and that is a wrapper with two proofs behind it: one that the transcription is the file''s own arrays, parsed back off the disk; one that the wrapper at its defaults gives the file''s own bytes. Gates at landing: check silent, build clean, 14/14 TRUE against a stub DOM written from nothing in the proof file — no jsdom, no happy-dom, no dependency at all. The proofs show the paused animation''s matrix and opacity landing inline, the pan-and-zoom and every class stripped from the picture and still standing on the live tree, the defs carried ahead of the world so a url(#…) still resolves once the picture has left the document, an SVG document byte-stable across two builds, a data URL decoding back to exactly those characters, fractional bounds exact to the fraction and the pixel box rounded outward, the road walked end to end onto a stub canvas for bytes that isPng accepts, and the four plain sentences this water fails with. HONEST LIMITS, SAID AND NOT SOFTENED: no browser has run any of this — the stub answers the way the spec says a browser answers, but a real getComputedStyle and a real XMLSerializer have not been asked; fonts do not travel, only their names; external url(...) references other than defs do not travel; and the build normalizes the extensionless relative import in its own EMITTED JavaScript, never in src/, because Node''s loader will not resolve what a bundler will and the copy is not edited. THE NAME IS A WORKING NAME AND HIS TO KEEP OR CHANGE — it lives in one constant and one folder so a rename is cheap.',
   'a builder dealt by Windrose, at KP''s ⚛ word');

-- THE CROSSING — his ⚛ word alone.
-- The standing ruling ("if a tool is working, it is flowing") argues for it
-- now that the proofs stand; uncomment at his word and run:
-- update public.awen set status = 'flowing' where slug = 'the-mynd';

-- ── verify (read-only, safe to re-run) ───────────────────────────────
select name, slug, tool_type, status, icon_emoji, folksonomy_type, home
from public.awen where slug = 'the-mynd';

select atom_word, category_name, atom_type, state, status, pascal_case
from public.atoms where atom_word = 'mynd';

-- no other row should wear 🏞️:
select icon_emoji, count(*) as wearers, string_agg(name, ' · ' order by name)
from public.awen where icon_emoji = '🏞️' group by icon_emoji;

-- 🖼️ is the doubled face this row set down — it should still have exactly
-- its two wearers and no third:
select icon_emoji, count(*) as wearers, string_agg(name, ' · ' order by name)
from public.awen where icon_emoji = '🖼️' group by icon_emoji;

-- the companions the triggers should have made, standing empty:
select 'etymology' as table_name, atom_word, completion_progress::text as filled
from public.etymology where atom_word = 'mynd'
union all
select 'sensory_lexicon', atom_word, coalesce(emoji, '(empty)')
from public.sensory_lexicon where atom_word = 'mynd';
