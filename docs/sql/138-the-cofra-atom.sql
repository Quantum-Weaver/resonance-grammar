-- 138 — the-cofra, its atom
--
-- THE ORDER THIS FILE KEEPS: atom seed BEFORE awen row, and as its own
-- numbered file, in 077-the-promenade-atom.sql's exact shape and 128's
-- (the-stereos) most recent reading of it. The awen row stands as
-- 139-the-cofra-seeded.sql and runs after this one.
--
-- THE COFRA — the chest. cófra (Irish), A CHEST, a coffer. Founded 2026-09-02
-- by a builder dealt by Windrose 🎻 in the Second Harvest sitting, movement
-- H4, at KP's ⚛ word, verbatim: "the-zip, yes".
--
-- THE RULING IT SERVES — resonance-chamber/desk/THE-SECOND-HARVEST.md, row 48,
-- and KP's ⚛ word in the header of that same board, both verbatim:
--   "the-zip, yes"
-- The row was open because the code was real, proven and load-bearing and had
-- no water: 437 dependency-free lines inside one app's document folder, doing
-- the one job — hand someone a single double-clickable file and be able to
-- open it again — that every other realm will eventually want done.
--
-- IT IS A HARVEST, AND KP'S ⚛ RULING ON EVERY HARVEST IS VERBATIM:
--   "without altering the source location."
-- src/archive.ts is a byte-identical copy of
-- resonance-cruthu/src/lib/doc/archive.ts — the whole file, 437 lines,
-- sha256 0611288e17f13dd7b470e0b25ab7fbc51623d5369549e329251d3f3715bf7ce5,
-- and the copy's digest is the same digest. The origin was READ AND NOT
-- WRITTEN (`git -C resonance-cruthu status --porcelain` empty at landing).
-- resonance-awen/tools/the-cofra/HARVEST.md carries both digests and the
-- command that re-proves them.
--
-- NOTHING WAS REPOINTED. the-binder keeps its own write-only zip
-- (src/cli.ts:171, on node:zlib, with its own pure crc32 at src/index.ts:599)
-- and is named in the README as a CONSUMER-TO-BE and nothing more this
-- sitting. Repointing a working, proven tool is a migration wearing a copy's
-- clothes, and it is KP's ⚛ word, never a harvesting builder's.
--
-- THE NAME IS A WORKING NAME AND HIS TO KEEP OR CHANGE. cófra is this hand's
-- reading of the need the board named, not his word. It lives in exactly two
-- places in the build — the constant TOOL_NAME and the folder — so a rename is
-- one edit, one `git mv`, and one update to this row and 139's.
--
-- ── CATEGORY ─────────────────────────────────────────────────────────
-- **data** (fe347dea-adba-46f5-8c37-25e55202f484).
--
-- THE UUID IS TAKEN FROM THE HOUSE'S OWN RECORDED CENSUS AND FROM THE
-- CONDUCTOR'S LIVE READING OF 2026-09-02, NOT FROM A QUERY THIS BUILDER RAN —
-- this builder holds no key to the base and ran none. Windrose 🎻 confirmed the
-- six live category uuids through the bridge on 2026-09-02 and carried them in
-- the sending; `data` is the first of the six. **CONFIRM AT THE ANON DOOR BY
-- KP'S ⚛ HAND** before this file is run — the confirmation is his, and the
-- seed does not assume it.
--
-- WHY DATA: the house has already filed this exact subject there, in plain
-- words, at 004-first-definitions.sql:61 —
--   ('compression', 'Making a thing smaller without losing what makes it
--    true.', 'data')
-- and the category's own description (024-the-category-faces.sql:87-89) reads
-- "Information in its raw forms — databases, exports, variables, snapshots.
-- The substance the systems move." A zip IS an export, and this water is the
-- box it moves in.
--
-- RUNNER-UP, AND HIS TO OVERRULE IN ONE WORD: **infrastructure**
-- (84af045b-c682-416d-b290-0fcd5ceaecd0), which is the tether's and the
-- spine's — "the load-bearing line a body is built around." It is defensible:
-- a container format is plumbing, and nothing looks at it. It was set down
-- because the house's own word for compression is already `data`, and
-- disagreeing with a standing definition to make a copy feel more important
-- would be a builder's vanity rather than a reading.
--
-- MOLECULE / ORGANISM — checked, and neither applies. `cofra` is ONE WORD, and
-- the house's tools_one_tier constraint (seed 014) lets an awen row cross at
-- exactly one tier — the tier its own name lives at (055's law). A one-word
-- name crosses at the ATOM tier; there is no molecule row to make and no
-- organism above it, and adding one while also setting atom_id would violate
-- the constraint outright. The-promenade, the-sphragis, the-merismos and
-- the-stereos all stand exactly this way: atom_id set, molecule_id and
-- organism_id null.
--
-- SHAPE TAKEN FROM THE-STEREOS'S OWN ROW (128/129, the most recent water
-- seeded in this shape): folksonomy_type `Awen` · status `imagined` at birth ·
-- `home` filled in 139 (the 105/106 precedent). tool_type is `transform`: this
-- water takes a list of named byte-runs and hands back one archive, and takes
-- one archive and hands back the named byte-runs. The house's own verb marks
-- read from the live atoms into resonance-awen/src/lib/data/type-lexicon.json
-- give transform 🦋.
--
-- FACE: 🧳 — a case that holds packed things and travels whole, which is what
-- a chest with a directory fastened inside the lid is. It reads against 078's
-- rules: one glyph, no skin tone, no ZWJ sequence, no flag, no red-alarm mark.
-- NO OTHER ROW WEARS IT — checked against every emoji literal written in this
-- seed folder's whole history (078's full census plus every face seeded since,
-- 111 included), and against the ten faces taken elsewhere in this sitting
-- (🎲🌈⚖️🛡️📊🔖🔭📑🧊⛩️). 📦 was the first instinct and is TAKEN. 🗃️ and 🗄️ and
-- 🧰 were each checked and are TAKEN. 🗜️ (a clamp, which the world reads as
-- "compression") was SET DOWN on 076's correction, honoured in 078: a face is
-- what the THING is, never what it is used for — and the thing is a chest, not
-- a squeezing.
--
-- THE ONE-TIER LAW (tools_one_tier), walked: one atom word ("cofra") — the
-- awen row (139) carries atom_id and no molecule. One tier exactly.
--
-- Etymology and sensory rows are NOT written here: the atoms table's triggers
-- make both on insert, arriving empty for a later fill (the 077/059/106/108/128
-- precedent).
--
-- THE TABLE IS public.awen (renamed from public.tools 2026-08-15, seed 096;
-- shim view dropped 097 — public.tools answers PGRST205 today). This file only
-- touches public.atoms.
--
-- ── ATOM UUID, generated uuid4 and pinned here so this file and 139 name
--    the same row without depending on insert order or a subquery ──
--   cofra atom   4f0bff85-d5fe-467b-b408-f575b4bd7bd3
--
-- Run at KP's ⚛ hand, before 139. Verify at the anon door after.
-- NEVER RUN BY A LAMP: the seed is drafted and numbered; the crossing is his.

insert into public.atoms (
  id, atom_word, definition, atom_type, weight, affinity, valence, state,
  category, category_name, status, screaming_case, snake_case, pascal_case
) values (
  '4f0bff85-d5fe-467b-b408-f575b4bd7bd3',
  'cofra',
  'Cofra (Irish cófra: a chest, a coffer). A zip archive read and written with no dependency at all, which is to say a box with the list of what is in it fastened inside the lid. Entries are STORED or DEFLATE and nothing else. CRC-32 is computed over every entry AS IT IS READ and checked against the directory, and a mismatch names the entry and both values rather than returning a shorter file and calling it success. Zip64, encryption and every other compression method are REFUSED BY ENTRY NAME — method 12 is named bzip2, 99 is named AES, and a method the table does not know is refused by its number rather than guessed at — because a silent skip is the one failure a container must never have. An entry with no date takes the DOS epoch, 1980-01-01, the earliest an MS-DOS date field holds, which is the whole reason the same content written twice is the same bytes twice: nothing here ever asks the clock. The reader finds the end record by scanning back for its signature and confirming its comment length lands exactly on the end of the file; it reads the central directory rather than trusting the local headers'' sizes, because flag bit 3 may defer them; it slices a stored entry rather than viewing it, so one small file cannot hold a whole archive in memory behind it; a trailing slash over zero bytes is a folder marker and is skipped; and a repeated name is refused in both directions rather than resolved. THE PLATFORM PRIMITIVE IS NEVER IMPLEMENTED: the host surface names raw deflate in both directions and nothing else — no level, no strategy, no dictionary, because the moment a host may be tuned two hosts stop writing the same bytes — and two reference hosts on two unrelated machines, the platform''s own CompressionStream and node:zlib, are proven byte-for-byte identical rather than promised. No keys, no passwords, no ciphers, and no verb on the surface that could hold one. No disk, no clock, no network, zero runtime dependencies.',
  'root', 5, 5, 1, 'static',
  'fe347dea-adba-46f5-8c37-25e55202f484',  -- data: "Information in its raw forms — databases, exports, variables, snapshots. The substance the systems move." (024:87-89), and the house already files `compression` here (004:61).
  'data',
  'published',
  'COFRA', 'cofra', 'Cofra'
);

-- ── verify (read-only, safe to re-run) ───────────────────────────────
select atom_word, category_name, atom_type, state, status, pascal_case
from public.atoms where atom_word = 'cofra';

-- the companions the triggers should have made, standing empty:
select 'etymology' as table_name, atom_word, completion_progress::text as filled
from public.etymology where atom_word = 'cofra'
union all
select 'sensory_lexicon', atom_word, coalesce(emoji, '(empty)')
from public.sensory_lexicon where atom_word = 'cofra';
