-- 117 — the-nomos seeded
--
-- THE NOMOS — the law, parsed. Built 2026-09-02 by a builder dealt by
-- Windrose 🎻 at KP's ⚛ word, verbatim: "hold the Daedalus work and proceed
-- with the rest, we will do it on its own." Movement A1 of the Unnamed Waters
-- sitting, board row 37.
--
-- THE DISTINCTION THE BOARD DREW, verbatim: "the policy parser, DISTINCT from
-- the-scrolls: this parses, those render." The water never imports it, and
-- proof 12 checks that as arithmetic rather than promising it — every .ts
-- under src/ read, no import reaching outside the folder, and "the-scrolls" on
-- no import line and in neither harvested file.
--
-- THE HARVEST RULING — KP ⚛, verbatim, board row 33: "without altering the
-- source location." AudHDities/src/lib/markdown/parsePrivacy.ts (131 lines)
-- and parseTerms.ts (76 lines) were copied byte-verbatim; AudHDities is
-- untouched and still runs both on two live pages. HARVEST.md in the folder
-- records the byte counts and the SHA256 of each copy, and verify.mjs proof 11
-- re-checks both digests on every run.
--
-- THREE LIVE FAULTS FOUND AND NOT ONE MENDED — 116's header carries them whole
-- with the origin's own line numbers: the table that loses its header row
-- (parsePrivacy.ts:64-67, :71-72), the subsection body filed under its parent
-- (parsePrivacy.ts:105-107 AND parseTerms.ts:50-52), and the terms parser that
-- can never read its own document's title and defaults its date
-- (parseTerms.ts:21-22). All three are proven exactly as they behave. Mending
-- them is a ruling about two live AudHDities pages, and it is KP's.
--
-- TWO PARSERS, ONE WATER, DELIBERATELY NOT MERGED: merging them means choosing
-- whose behaviour wins across two live pages. Their differences are PROVEN
-- (proofs 6 and 7) rather than smoothed over.
--
-- CATEGORY: **governance** (ba101ebf-34fd-44d3-956f-0639e3367e73) — category
-- taken from seed 108's recorded census; confirmed at the anon door by KP's
-- hand. See 116's header for the definition matched and the `content`
-- runner-up left there for him to overrule in one word.
--
-- TOOL_TYPE: **translate** — it carries one form into another without adding
-- or deciding anything, which is what the translate family does. RUNNER-UP AND
-- HIS TO OVERRULE IN ONE WORD: `transform`, defensible because prose goes in
-- and a tree comes out; translate was taken because NOTHING IS ADDED — the
-- output says exactly what the document said and nothing else.
--
-- FACE: ⚖️ — the scales. One glyph, no skin tone, no ZWJ, no flag, no
-- red-alarm mark, and no awen row wears it. It IS worn by the `governance`
-- CATEGORY (024) and by the atom `comparison` (004), deliberately: 088's
-- precedent is "the atom and its category wear the same mark", and this row's
-- category is governance. 078's shared-face test does not arise — no awen row
-- shares it.
--
-- THE ONE-TIER LAW (tools_one_tier), walked: one atom word ("nomos") — the row
-- carries atom_id and no molecule. One tier exactly.
--
-- Etymology and sensory rows are NOT written here (triggers make them, 116).
--
-- THE TABLE IS public.awen (096; shim dropped 097).
--
-- Local mirror pin (progenatrix): fd182a01-3488-45e3-bbc0-d7a39ceb3ead
--
-- GATE AT LANDING, run from a clean shell before this file was written:
--   npm install && npm run build && npm run check && npm run prove
--   → check silent, 12/12 TRUE, deterministic, no clock and no wire
--
-- Run at KP's ⚛ hand, after 116. Verify at the anon door after.

insert into public.awen
  (name, slug, tool_type, definition, home, status, icon_emoji, folksonomy_type, atom_id, story, created_by)
values
  ('The Nomos', 'the-nomos', 'translate',
   'The law, parsed — a policy document turned into columns. DISTINCT FROM THE-SCROLLS: THOSE RENDER, THIS PARSES, and it never imports them; the law of the spring is checked in its own proofs as arithmetic rather than promised. It takes the markdown a founder or a lawyer actually wrote and hands back its sections, its subsections, its tables and the date it states, as DATA, with no opinion about how any of it looks. THE DATE IS THE DOCUMENT''S OR IT IS EMPTY: an undated policy returns an empty string and never a default, because a hero printing a hardcoded date for a document that does not carry one is telling the reader something the document never said — and BOTH of the house''s labels are read, "Last updated:" for the docs policy and "Effective date:" for the shipped app policies. A TABLE IS LIFTED, NOT FLATTENED — it comes back as headers and rows on its own section, so a reader can put it in a real table instead of re-parsing pipes on a screen. NESTING IS KEPT, and the tree is the document''s own. NOTHING THROWS AND NOTHING IS INVENTED across every shape that is not a policy: an empty string, no headings at all, a heading with no body, a subheading with no parent (DROPPED rather than promoted to a section it does not belong to), a table outside any section, a header-less table (which attaches nothing rather than an empty shape pretending to be one) and a ragged table (taken exactly as it stands, un-padded and un-refused, because a document with a ragged table HAS a ragged table). Two parsers ride here, privacy and terms, deliberately NOT merged, because merging them means choosing whose behaviour wins across two live pages — a ruling, not a tidy-up — and their differences are proven rather than smoothed. THREE LIVE FAULTS IN THE ORIGIN ARE CARRIED WHOLE AND TOLD RATHER THAN MENDED: the table loses its header row to the separator line, a subsection''s body is filed under its parent in BOTH parsers, and the terms parser can never read its own document''s title and still defaults its date to March 19 2026. No clock, no disk, no network, no DOM, zero imports; reading the file stays the caller''s business.',
   'resonance-awen/tools/the-nomos', 'imagined', '⚖️', 'Awen',
   'c7150517-3960-4667-95cc-c7ef45fe8fd8',  -- the nomos atom, seeded in 116
   'Built 2026-09-02 by a builder dealt by Windrose 🎻, at KP''s ⚛ word "hold the Daedalus work and proceed with the rest, we will do it on its own" — movement A1 of the Unnamed Waters sitting, four waters from the harvests, this one the third. HARVESTED UNDER HIS OWN RULING, verbatim: "without altering the source location" — AudHDities/src/lib/markdown/parsePrivacy.ts (131 lines) and parseTerms.ts (76 lines) copied BYTE-VERBATIM, zero lines changed, AudHDities untouched and still running both on two live pages, and the SHA256 of each copy pinned in the water''s proofs and re-checked every run. THE FIXTURE IS REAL HOUSE GROUND, BOUNDED AND CITED: the proofs run on excerpts transcribed from the documents the origin''s own pages feed these parsers — docs/privacy/privacy.md (123 lines, :1-40 transcribed, through the end of the Legal Basis table) and docs/terms/terms-of-service.md (119 lines, :1-29) — found by following src/app/(hephaestus)/privacy/page.tsx:19 and terms/page.tsx:18, each of which reads the file and hands the string straight to the parser. They are transcribed rather than read off disk because a water given away whole cannot have a proof that needs another repo present. THREE LIVE FAULTS WERE FOUND AND NONE WAS MENDED — the lost table header (parsePrivacy.ts:64-67, :71-72), the subsection body filed under its parent (parsePrivacy.ts:105-107 and parseTerms.ts:50-52), and the terms parser that can never read its own title and defaults its date (parseTerms.ts:21-22) — each proven exactly as it behaves, with its address, in verify.mjs and in HARVEST.md and in the README. Whether to mend them is a ruling about two live pages and it is HIS. Gate at landing from a clean shell: check silent, 12/12 TRUE. THE NAME IS A WORKING NAME AND HIS TO KEEP OR CHANGE.',
   'a builder dealt by Windrose 🎻, at KP''s ⚛ word');

-- THE CROSSING — his ⚛ word alone.
-- The standing ruling ("if a tool is working, it is flowing") argues for it
-- now that the proofs stand; uncomment at his word and run:
-- update public.awen set status = 'flowing' where slug = 'the-nomos';

-- ── verify (read-only, safe to re-run) ───────────────────────────────
select name, slug, tool_type, status, icon_emoji, folksonomy_type, home
from public.awen where slug = 'the-nomos';

select atom_word, category_name, atom_type, state, status, pascal_case
from public.atoms where atom_word = 'nomos';

-- no other row should wear ⚖️:
select icon_emoji, count(*) as wearers, string_agg(name, ' · ' order by name)
from public.awen where icon_emoji = '⚖️' group by icon_emoji;

-- the companions the triggers should have made, standing empty:
select 'etymology' as table_name, atom_word, completion_progress::text as filled
from public.etymology where atom_word = 'nomos'
union all
select 'sensory_lexicon', atom_word, coalesce(emoji, '(empty)')
from public.sensory_lexicon where atom_word = 'nomos';
