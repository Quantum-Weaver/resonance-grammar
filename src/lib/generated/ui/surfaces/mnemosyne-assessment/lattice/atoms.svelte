<!--
	=====================================================
	GENERATED SURFACE - DO NOT EDIT MANUALLY
	=====================================================
	table: atoms · deity: lattice · base: knowledge
	source: atom_dressed (card) · columns: 17
	face: quantum.light → #7D6CEA
	schema_hash: a137f60d823cd6ce6fd83cf366fd1833 · source sha256 36d8c6da148b
	Generated by resonance-gaia src/pieces/generate_surface_file.ts
	A hand composed this card in SQL. These columns, this order — to change it, change the view.
	=====================================================
-->
<script module lang="ts">
	/** the table this surface faces */
	export const TABLE = "atoms";
	/** the room it belongs to */
	export const DEITY = "lattice";
	/** THE RELATION TO READ — a loader asks the base for exactly this */
	export const SOURCE = "atom_dressed";
	/** 'card' when a composed view decided the columns, 'shelf' when nobody has */
	export const SURFACE_KIND = "card";
	/** the columns, in the order they are rendered — the whole contract */
	export const COLUMNS: string[] = [
		"atom_id",
		"atom_word",
		"snake_case",
		"screaming_case",
		"pascal_case",
		"folksonomy_type",
		"definition",
		"emoji",
		"color_hex",
		"category_name",
		"category_face",
		"atom_type",
		"modifiers",
		"root_language",
		"root_word",
		"sanctuary_meaning",
		"is_override",
	];
</script>

<script lang="ts">
	import { QUANTUM_COLORS } from '$lib/cosmic';

	type Row = Record<string, unknown>;

	let {
		rows = [],
		loading = false,
		error = null,
		title = TABLE,
	}: {
		rows?: Row[];
		loading?: boolean;
		error?: string | null;
		title?: string;
	} = $props();

	// the room's face, by cosmic token — never a hex literal in a stylesheet
	const accent = QUANTUM_COLORS['quantum.light'];
	const emoji = "💠";

	// the base's own words, rendered as they are: null reads as an em dash,
	// json as json. Nothing here decides a column is uninteresting.
	function display(value: unknown): string {
		if (value === null || value === undefined) return '—';
		if (typeof value === 'string') return value;
		if (typeof value === 'number' || typeof value === 'boolean') return String(value);
		return JSON.stringify(value);
	}

	// atom_dressed does not carry the table's primary key — key on position
	const KEY_COLUMNS: string[] = [];

	function keyOf(row: Row, index: number): string {
		if (KEY_COLUMNS.length === 0) return String(index);
		return KEY_COLUMNS.map((c) => display(row[c])).join('|');
	}

	function haystack(row: Row): string {
		return COLUMNS.map((c) => display(row[c])).join(' ').toLowerCase();
	}

	let query = $state('');

	const filtered = $derived.by(() => {
		const needle = query.trim().toLowerCase();
		if (needle === '') return rows;
		return rows.filter((row) => haystack(row).includes(needle));
	});
</script>

<section class="surface surface--card" style="--accent: {accent}">
	<header class="surface__head">
		<span class="surface__emoji" aria-hidden="true">{emoji}</span>
		<h2 class="surface__title">{title}</h2>
		<span class="surface__source">{SOURCE}</span>
	</header>

	<label class="surface__search">
		<span class="surface__search-label">Search {TABLE}</span>
		<input type="search" bind:value={query} placeholder="Filter these rows…" />
	</label>

	{#if error}
		<p class="surface__note surface__note--error" role="alert">{error}</p>
	{:else if loading}
		<p class="surface__note">Reading {SOURCE}…</p>
	{:else if filtered.length === 0}
		<p class="surface__note">
			{rows.length === 0 ? 'Nothing on this card yet.' : 'No row matches that.'}
		</p>
	{:else}
		<div class="surface__cards">
			{#each filtered as row, index (keyOf(row, index))}
				<article class="card">
					<dl class="card__fields">
						<div class="card__field">
							<dt class="card__label">atom_id</dt>
							<dd class="card__value">{display(row["atom_id"])}</dd>
						</div>
						<div class="card__field">
							<dt class="card__label">atom_word</dt>
							<dd class="card__value">{display(row["atom_word"])}</dd>
						</div>
						<div class="card__field">
							<dt class="card__label">snake_case</dt>
							<dd class="card__value">{display(row["snake_case"])}</dd>
						</div>
						<div class="card__field">
							<dt class="card__label">screaming_case</dt>
							<dd class="card__value">{display(row["screaming_case"])}</dd>
						</div>
						<div class="card__field">
							<dt class="card__label">pascal_case</dt>
							<dd class="card__value">{display(row["pascal_case"])}</dd>
						</div>
						<div class="card__field">
							<dt class="card__label">folksonomy_type</dt>
							<dd class="card__value">{display(row["folksonomy_type"])}</dd>
						</div>
						<div class="card__field">
							<dt class="card__label">definition</dt>
							<dd class="card__value">{display(row["definition"])}</dd>
						</div>
						<div class="card__field">
							<dt class="card__label">emoji</dt>
							<dd class="card__value">{display(row["emoji"])}</dd>
						</div>
						<div class="card__field">
							<dt class="card__label">color_hex</dt>
							<dd class="card__value">{display(row["color_hex"])}</dd>
						</div>
						<div class="card__field">
							<dt class="card__label">category_name</dt>
							<dd class="card__value">{display(row["category_name"])}</dd>
						</div>
						<div class="card__field">
							<dt class="card__label">category_face</dt>
							<dd class="card__value">{display(row["category_face"])}</dd>
						</div>
						<div class="card__field">
							<dt class="card__label">atom_type</dt>
							<dd class="card__value">{display(row["atom_type"])}</dd>
						</div>
						<div class="card__field">
							<dt class="card__label">modifiers</dt>
							<dd class="card__value">{display(row["modifiers"])}</dd>
						</div>
						<div class="card__field">
							<dt class="card__label">root_language</dt>
							<dd class="card__value">{display(row["root_language"])}</dd>
						</div>
						<div class="card__field">
							<dt class="card__label">root_word</dt>
							<dd class="card__value">{display(row["root_word"])}</dd>
						</div>
						<div class="card__field">
							<dt class="card__label">sanctuary_meaning</dt>
							<dd class="card__value">{display(row["sanctuary_meaning"])}</dd>
						</div>
						<div class="card__field">
							<dt class="card__label">is_override</dt>
							<dd class="card__value">{display(row["is_override"])}</dd>
						</div>
					</dl>
				</article>
			{/each}
		</div>
	{/if}

	<footer class="surface__foot">
		{filtered.length} of {rows.length} · {COLUMNS.length} columns from {SOURCE}
	</footer>
</section>

<style>
	.surface {
		display: flex;
		flex-direction: column;
		gap: 0.75rem;
		padding: 1rem;
		border-left: 3px solid var(--accent);
		border-radius: 0.5rem;
		background: color-mix(in srgb, var(--accent) 6%, transparent);
	}

	.surface__head {
		display: flex;
		align-items: baseline;
		gap: 0.5rem;
		flex-wrap: wrap;
	}

	.surface__emoji {
		font-size: 1.25rem;
	}

	.surface__title {
		margin: 0;
		font-size: 1.1rem;
		font-weight: 600;
		color: var(--accent);
	}

	.surface__source {
		font-family: ui-monospace, monospace;
		font-size: 0.75rem;
		opacity: 0.6;
	}

	.surface__search {
		display: block;
	}

	.surface__search-label {
		position: absolute;
		width: 1px;
		height: 1px;
		overflow: hidden;
		clip-path: inset(50%);
		white-space: nowrap;
	}

	.surface__search input {
		width: 100%;
		padding: 0.4rem 0.6rem;
		border: 1px solid color-mix(in srgb, var(--accent) 35%, transparent);
		border-radius: 0.375rem;
		background: transparent;
		color: inherit;
		font: inherit;
	}

	.surface__note {
		margin: 0;
		padding: 0.75rem 0;
		opacity: 0.7;
	}

	.surface__note--error {
		color: var(--accent);
		opacity: 1;
	}

	.surface__foot {
		font-size: 0.72rem;
		opacity: 0.55;
	}

	.surface__cards {
		display: grid;
		grid-template-columns: repeat(auto-fill, minmax(16rem, 1fr));
		gap: 0.75rem;
	}

	.card {
		padding: 0.75rem;
		border: 1px solid color-mix(in srgb, var(--accent) 28%, transparent);
		border-radius: 0.5rem;
	}

	.card__fields {
		margin: 0;
		display: flex;
		flex-direction: column;
		gap: 0.35rem;
	}

	.card__label {
		font-family: ui-monospace, monospace;
		font-size: 0.7rem;
		text-transform: none;
		opacity: 0.6;
	}

	.card__value {
		margin: 0;
		overflow-wrap: anywhere;
	}
</style>
