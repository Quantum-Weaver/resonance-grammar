<!--
	=====================================================
	GENERATED SURFACE - DO NOT EDIT MANUALLY
	=====================================================
	table: beacons · deity: workshop · base: knowledge
	source: beacons_dressed (card) · columns: 20
	face: fire.light → #F18166
	schema_hash: f7e86e070ea5a1544714c61c7423dafc · source sha256 36d8c6da148b
	Generated by resonance-gaia src/pieces/generate_surface_file.ts
	A hand composed this card in SQL. These columns, this order — to change it, change the view.
	=====================================================
-->
<script module lang="ts">
	/** the table this surface faces */
	export const TABLE = "beacons";
	/** the room it belongs to */
	export const DEITY = "workshop";
	/** THE RELATION TO READ — a loader asks the base for exactly this */
	export const SOURCE = "beacons_dressed";
	/** 'card' when a composed view decided the columns, 'shelf' when nobody has */
	export const SURFACE_KIND = "card";
	/** the columns, in the order they are rendered — the whole contract */
	export const COLUMNS: string[] = [
		"id",
		"name",
		"slug",
		"face",
		"own_face",
		"beacon_type",
		"type_face",
		"type_colour",
		"named_for",
		"named_for_face",
		"named_for_colour",
		"named_via",
		"status",
		"family",
		"repo_url",
		"is_public",
		"version",
		"available_on",
		"definition",
		"story",
	];
</script>

<script lang="ts">
	import { QUANTUM_COLORS } from '$lib/cosmic';

	type Row = Record<string, unknown>;

	let {
		rows = [],
		loading = false,
		error = null,
		title = TABLE,
	}: {
		rows?: Row[];
		loading?: boolean;
		error?: string | null;
		title?: string;
	} = $props();

	// the room's face, by cosmic token — never a hex literal in a stylesheet
	const accent = QUANTUM_COLORS['fire.light'];
	const emoji = "🧰";

	// the base's own words, rendered as they are: null reads as an em dash,
	// json as json. Nothing here decides a column is uninteresting.
	function display(value: unknown): string {
		if (value === null || value === undefined) return '—';
		if (typeof value === 'string') return value;
		if (typeof value === 'number' || typeof value === 'boolean') return String(value);
		return JSON.stringify(value);
	}

	// beacons_dressed carries the whole primary key — key the list on it
	const KEY_COLUMNS: string[] = ["id"];

	function keyOf(row: Row, index: number): string {
		if (KEY_COLUMNS.length === 0) return String(index);
		return KEY_COLUMNS.map((c) => display(row[c])).join('|');
	}

	function haystack(row: Row): string {
		return COLUMNS.map((c) => display(row[c])).join(' ').toLowerCase();
	}

	let query = $state('');

	const filtered = $derived.by(() => {
		const needle = query.trim().toLowerCase();
		if (needle === '') return rows;
		return rows.filter((row) => haystack(row).includes(needle));
	});
</script>

<section class="surface surface--card" style="--accent: {accent}">
	<header class="surface__head">
		<span class="surface__emoji" aria-hidden="true">{emoji}</span>
		<h2 class="surface__title">{title}</h2>
		<span class="surface__source">{SOURCE}</span>
	</header>

	<label class="surface__search">
		<span class="surface__search-label">Search {TABLE}</span>
		<input type="search" bind:value={query} placeholder="Filter these rows…" />
	</label>

	{#if error}
		<p class="surface__note surface__note--error" role="alert">{error}</p>
	{:else if loading}
		<p class="surface__note">Reading {SOURCE}…</p>
	{:else if filtered.length === 0}
		<p class="surface__note">
			{rows.length === 0 ? 'Nothing on this card yet.' : 'No row matches that.'}
		</p>
	{:else}
		<div class="surface__cards">
			{#each filtered as row, index (keyOf(row, index))}
				<article class="card">
					<dl class="card__fields">
						<div class="card__field">
							<dt class="card__label">id</dt>
							<dd class="card__value">{display(row["id"])}</dd>
						</div>
						<div class="card__field">
							<dt class="card__label">name</dt>
							<dd class="card__value">{display(row["name"])}</dd>
						</div>
						<div class="card__field">
							<dt class="card__label">slug</dt>
							<dd class="card__value">{display(row["slug"])}</dd>
						</div>
						<div class="card__field">
							<dt class="card__label">face</dt>
							<dd class="card__value">{display(row["face"])}</dd>
						</div>
						<div class="card__field">
							<dt class="card__label">own_face</dt>
							<dd class="card__value">{display(row["own_face"])}</dd>
						</div>
						<div class="card__field">
							<dt class="card__label">beacon_type</dt>
							<dd class="card__value">{display(row["beacon_type"])}</dd>
						</div>
						<div class="card__field">
							<dt class="card__label">type_face</dt>
							<dd class="card__value">{display(row["type_face"])}</dd>
						</div>
						<div class="card__field">
							<dt class="card__label">type_colour</dt>
							<dd class="card__value">{display(row["type_colour"])}</dd>
						</div>
						<div class="card__field">
							<dt class="card__label">named_for</dt>
							<dd class="card__value">{display(row["named_for"])}</dd>
						</div>
						<div class="card__field">
							<dt class="card__label">named_for_face</dt>
							<dd class="card__value">{display(row["named_for_face"])}</dd>
						</div>
						<div class="card__field">
							<dt class="card__label">named_for_colour</dt>
							<dd class="card__value">{display(row["named_for_colour"])}</dd>
						</div>
						<div class="card__field">
							<dt class="card__label">named_via</dt>
							<dd class="card__value">{display(row["named_via"])}</dd>
						</div>
						<div class="card__field">
							<dt class="card__label">status</dt>
							<dd class="card__value">{display(row["status"])}</dd>
						</div>
						<div class="card__field">
							<dt class="card__label">family</dt>
							<dd class="card__value">{display(row["family"])}</dd>
						</div>
						<div class="card__field">
							<dt class="card__label">repo_url</dt>
							<dd class="card__value">{display(row["repo_url"])}</dd>
						</div>
						<div class="card__field">
							<dt class="card__label">is_public</dt>
							<dd class="card__value">{display(row["is_public"])}</dd>
						</div>
						<div class="card__field">
							<dt class="card__label">version</dt>
							<dd class="card__value">{display(row["version"])}</dd>
						</div>
						<div class="card__field">
							<dt class="card__label">available_on</dt>
							<dd class="card__value">{display(row["available_on"])}</dd>
						</div>
						<div class="card__field">
							<dt class="card__label">definition</dt>
							<dd class="card__value">{display(row["definition"])}</dd>
						</div>
						<div class="card__field">
							<dt class="card__label">story</dt>
							<dd class="card__value">{display(row["story"])}</dd>
						</div>
					</dl>
				</article>
			{/each}
		</div>
	{/if}

	<footer class="surface__foot">
		{filtered.length} of {rows.length} · {COLUMNS.length} columns from {SOURCE}
	</footer>
</section>

<style>
	.surface {
		display: flex;
		flex-direction: column;
		gap: 0.75rem;
		padding: 1rem;
		border-left: 3px solid var(--accent);
		border-radius: 0.5rem;
		background: color-mix(in srgb, var(--accent) 6%, transparent);
	}

	.surface__head {
		display: flex;
		align-items: baseline;
		gap: 0.5rem;
		flex-wrap: wrap;
	}

	.surface__emoji {
		font-size: 1.25rem;
	}

	.surface__title {
		margin: 0;
		font-size: 1.1rem;
		font-weight: 600;
		color: var(--accent);
	}

	.surface__source {
		font-family: ui-monospace, monospace;
		font-size: 0.75rem;
		opacity: 0.6;
	}

	.surface__search {
		display: block;
	}

	.surface__search-label {
		position: absolute;
		width: 1px;
		height: 1px;
		overflow: hidden;
		clip-path: inset(50%);
		white-space: nowrap;
	}

	.surface__search input {
		width: 100%;
		padding: 0.4rem 0.6rem;
		border: 1px solid color-mix(in srgb, var(--accent) 35%, transparent);
		border-radius: 0.375rem;
		background: transparent;
		color: inherit;
		font: inherit;
	}

	.surface__note {
		margin: 0;
		padding: 0.75rem 0;
		opacity: 0.7;
	}

	.surface__note--error {
		color: var(--accent);
		opacity: 1;
	}

	.surface__foot {
		font-size: 0.72rem;
		opacity: 0.55;
	}

	.surface__cards {
		display: grid;
		grid-template-columns: repeat(auto-fill, minmax(16rem, 1fr));
		gap: 0.75rem;
	}

	.card {
		padding: 0.75rem;
		border: 1px solid color-mix(in srgb, var(--accent) 28%, transparent);
		border-radius: 0.5rem;
	}

	.card__fields {
		margin: 0;
		display: flex;
		flex-direction: column;
		gap: 0.35rem;
	}

	.card__label {
		font-family: ui-monospace, monospace;
		font-size: 0.7rem;
		text-transform: none;
		opacity: 0.6;
	}

	.card__value {
		margin: 0;
		overflow-wrap: anywhere;
	}
</style>
