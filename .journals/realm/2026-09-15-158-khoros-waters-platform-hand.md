# 158 — the khorós waters seeded

## Written

`docs/sql/158-the-khoros-waters-seeded.sql` — one insert of four `public.awen`
rows at status `imagined`, from `resonance-khoros/docs/THE-KHOROS-WATERS.md`
rows W1 to W4, plus five read-only verify selects.

| slug | atom | tool_type | face |
|---|---|---|---|
| the-offer | offer | combine | 🎟️ |
| the-follow | follow | arrange | 📡 |
| the-measure | measure | witness | 📀 |
| the-sidecar | sidecar | transform | 🛰️ |

Each row carries `atom_id` by subquery, `molecule_id` and `organism_id` null,
`home` `resonance-awen/tools/<slug>`, `folksonomy_type` `Awen`, `created_by`
`a platform hand, at KP's ⚛ word`.

## Checked

- `python ../resonance-nectere/chains/the-seed-chain/seed_chain.py run docs/sql/158-the-khoros-waters-seeded.sql --base knowledge` — `statements: 28 · DRY RUN`. Six of the 28 are statement terminators; 22 are semicolons inside string literals.
- Every string literal scanned: the text outside the literals is pure SQL, so every apostrophe inside a string is doubled.
- Definitions 124, 103, 95 and 91 words.
- The four slugs are absent from the 105 the spring wears; the four atom words
  all stand in `public.atoms` and none is born in 153; the four faces are from
  158's pool, each worn once, none in the worn-face list and none in a sibling
  paper 153 to 160.
- No `update`, no crossing, no delete. Nothing was delivered.

## Not fitted to a row

- 0.19 — the host and the subdomain — is a ruling and a machine, not data.
- The carry road's client half, the ordered many-works membership (item 143),
  the live room and the stream descriptor, and a release manifest as one wide
  document: the document rejects each as a new water.
- The consumption sitting (step 3 of the document's order) mirrors
  the-catalogue-raisonne, the-release-model, the-player and the-listening-room
  into Khorós; those waters already stand.
- Step 5 mirrors the-sruth and the-ceilidh in and writes the Stream hat's body;
  no new water.
