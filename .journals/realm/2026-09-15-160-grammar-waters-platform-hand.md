# 160, the grammar waters seeded — 2026-09-15 platform-hand

*Nothing committed. Nothing delivered. One paper added, this entry beside it.*

## What the work did

`docs/sql/160-the-grammar-waters-seeded.sql` — written new. One `insert` into
`public.awen` carrying seven rows at status `imagined`, and four read-only
verify selects. Five statements. No existing paper was edited and no base was
written.

| slug | atom | tool_type | face |
|---|---|---|---|
| the-cases | case (stands, takes "s" in 153) | transform | 🔣 |
| the-audit | audit (stands) | witness | ☑️ |
| the-closure | closure (born in 153) | witness | 🧬 |
| the-lattice | lattice (stands) | arrange | 🎐 |
| the-frame | frame (stands) | emit | 📋 |
| the-read | read (stands) | translate | 🔎 |
| the-mark | mark (stands) | witness | 🪶 |

Every row: `home` `resonance-awen/tools/<slug>` · `folksonomy_type` `Awen` ·
`atom_id` by subquery on `atom_word`, `molecule_id` and `organism_id` null.
Definitions are drawn from rows W1 to W7 of `docs/THE-GRAMMAR-WATERS.md` and
carry no file addresses. Stories carry that document's own addresses, the
consumers, the mark and the plan row.

## What was read

`docs/THE-GRAMMAR-WATERS.md` whole · `docs/sql/152-the-patakatha-seeded.sql` ·
`docs/sql/055-the-twenty-waters.sql` · `README.md` head · `docs/STORY-BLOCK.md`
· `resonance-nectere/chains/the-seed-chain/seed_chain.py`.

## What was checked

    python ../resonance-nectere/chains/the-seed-chain/seed_chain.py run \
      docs/sql/160-the-grammar-waters-seeded.sql --base knowledge
    base: knowledge · project ref: qdzerwmsbksuhvczlwli
    statements: 5 · DRY RUN
    DRY RUN — nothing sent.

By reading: the seven slugs against the 105 the spring wears — no collision ·
every atom word in the standing list, `closure` alone born in 153 · the seven
faces all from this paper's pool, each worn once, none in the 105 rows' faces
and none in another paper's pool · `status` `imagined` on all seven · every
apostrophe inside a string doubled, checked by quote parity on every
non-comment line and by tokenising the insert, which yields 7 tuples of 13
fields · no semicolon inside any string, so the statement count is the five
statements themselves · definitions 86 to 129 words.

## What did not fit a row

- 0.16 (`THE-FINAL-FEATURES-PLAN.md:681-682`) asks for no water, per the
  document's own reading, so no row answers it.
- The eight things the document names as deliberately not a water: the
  composition itself, the toolchain for the ten generated routes, a route
  manifest reader, the table's appearance in the yard (the-panti stands), the
  writes, the SQL papers and the new-table ritual, the base's own machinery,
  and the Rust CLI's local shape.
- The two-tongues question — a water wanted in python and in TypeScript either
  written twice or carrying laws as data with one reference implementation — is
  unsettled in the document for W1, W2 and W6. It is carried in the-cases'
  story as unsettled and is not a row.
- The twelve waters already serving are register rows already.
